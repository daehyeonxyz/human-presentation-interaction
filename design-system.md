# 디자인 시스템

이 레포의 모든 덱이 쓰는 값과 골격이다. 값의 기계 원본은 [tokens.css](tokens.css) 이고 이 문서는 그 값이 무엇을 뜻하고 어디에 쓰이는지를 정한다. 둘이 어긋나면 `tokens.css` 가 맞다.

출처는 AX 101 · Claude Zero to One 덱이다. 실물로 43쪽을 발표까지 끌고 간 체계이므로 값의 근거는 그 덱의 화면이다. 새 값을 만들 일이 생기면 이 문서와 `tokens.css` 를 함께 고치고, 덱에서 먼저 고치지 않는다.

레이아웃 패턴은 [layout-taxonomy.md](layout-taxonomy.md), 조작 문법은 [interaction-grammar.md](interaction-grammar.md) 가 갖는다. 이 문서는 색과 글자와 간격과 골격과 모션만 정한다.

## 사본 규칙

산출물은 외부 파일에 의존하지 않는 단일 HTML 이다. 그래서 `tokens.css` 를 `<link>` 로 걸지 않고 `<style>` 안에 복사한다.

1. `tokens.css` 를 고친다.
2. 덱의 `:root` 블록을 다시 복사한다.
3. 렌더해서 확인한다.

사본만 고친 값은 다음 복사에서 사라진다. 사본을 원본으로 승격시키고 싶으면 `tokens.css` 에 먼저 넣는다.

## 면

| 토큰 | 값 | 무엇 |
|---|---|---|
| `--paper` | `#F1F1F7` | 쪽의 바탕. 레터박스도 이 색이다 |
| `--paper-2` | `#E4E5EE` | 바탕 위의 덩이. 트랙, 비어 있는 칸 |
| `--paper-3` | `#D6D8E4` | 한 단 더 눌린 덩이. 잠긴 것, 꺼진 것 |
| `--white` | `#FFFFFF` | 종이 위에 뜬 면. 카드, 누르는 것 |
| `--night` | `#06091C` | 어두운 쪽 전체 캔버스 |

- 층은 밝기 차이가 만든다. 그림자로 만들지 않는다.
- `--shadow` 는 한 값뿐이고 카드가 종이에 닿은 자리만 표시한다. 띄우는 데 쓰지 않는다.
- 면 겹침은 두 겹까지다. 카드 안의 카드 안의 카드를 만들지 않는다.
- 어두운 쪽은 `.s.dark`, 강조색으로 꽉 채운 쪽은 `.s.blue` 다. 그 쪽에서는 `.viewport` 도 같은 색이 되어 레터박스가 쪽과 같은 색으로 남는다.

## 글자

크기와 행간은 짝이다. 크기만 고르고 행간을 따로 정하지 않는다.

| 토큰 | 크기 / 행간 | 굵기 | 쓰는 자리 |
|---|---|---|---|
| `--t-cap` | 26 / 36 | 500 | 캡션, 범례, 표 셀, 쪽 번호 |
| `--t-body` | 32 / 48 | 500 | 본문. `body` 의 기본값 |
| `--t-h2` | 40 / 56 | 700 | 소제목, 항목 이름 |
| `--t-h3` | 48 / 64 | 700 | 큰 항목 이름 |
| `--t-h` | 64 / 76 | 800 | 쪽 제목 |
| `--t-decl` | 96 / 112 | 800 | 선언 |
| `--t-decl-lg` | 120 / 136 | 800 | 큰 선언 |
| `--t-title` | 220 / 220 | 800 | 표지 제목 전용 |

- 한 쪽에 `--t-h` 이상인 글자는 하나다. 제목과 선언은 같은 쪽에 오지 않는다.
- 사다리 사이 값을 만들지 않는다. 36 이나 56 이 필요해 보이면 그 자리의 역할을 잘못 잡은 것이다.
- 자간은 크기가 정한다. 64 이상은 `-.02em`, 선언은 `-.025em`~`-.03em`, 40 은 `-.015em`, 그 아래는 0 이다.
- 굵기는 500 · 600 · 700 · 800 넷이다. 한글 본문의 하한은 500 이고 400 은 쓰지 않는다.
- 서체는 Pretendard 하나다. 라틴 전용 서체를 따로 두지 않는다. 한 줄 안에서 서체가 갈리는 것이 라틴 전용 서체의 실제 결과다.
- `word-break: keep-all` 을 스테이지 전체에 건다. 줄 나눔은 원고가 정하고 화면 폭에 맡기지 않는다.

## 간격

일곱 값과 여백 하나다. 이 밖의 간격을 쓰지 않는다.

| 토큰 | 값 | 쓰는 자리 |
|---|---|---|
| `--pad` | 80 | 쪽의 네 변 여백 |
| `--gap-3xs` | 4 | 붙어 있는 두 줄 |
| `--gap-2xs` | 8 | 한 덩이 안의 층 |
| `--gap-xs` | 12 | 머리와 몸 사이 |
| `--gap-sm` | 24 | 항목 사이 |
| `--gap` | 40 | 덩이 사이. 쪽의 기본 간격 |
| `--gap-lg` | 64 | 묶음 사이 |
| `--gap-xl` | 96 | 좌우로 가른 두 덩이 사이 |

- `--side` 560 은 좌우로 가를 때 좁은 쪽의 폭이다. `.cols.side` 가 쓴다.
- 칸이 남으면 간격을 늘리지 않고 그 칸에 들어갈 정보를 늘린다.

## radius

세 값뿐이다.

- `--r-card` 16 — 카드, 판, 큰 면
- `--r-ctl` 12 — 버튼, 입력, 알약, 누르는 조각
- `--r-dot` 4 — 막대 조각, 점, 작은 표식
- 원은 `border-radius: 50%` 로 직접 만든다. 알약은 `999px` 을 쓴다.

## 색

- 강조색은 `--acc` 하나다. 종류 구분이 필요하면 색상을 늘리지 않고 `--n1`~`--n7` 명도 계단을 옮긴다.
- `--acc-soft` 는 강조색의 옅은 면이다. 배지와 바탕에만 쓰고 그 위에 강조색 글자를 얹지 않는다.
- `--g1`~`--g4` 는 강조색이 아닌 것들의 회색 층이다. 도해에서 주인공이 아닌 것이 여기 있다.
- `--c1`~`--c5` 는 명도가 아니라 **종류**가 다를 때만 쓴다. `--c1` 은 `--acc` 를 따라간다. 프로젝트 강조색이 `--c2`(청록) `--c3`(보라) `--c4`(주황) 가운데 하나와 가까우면 그 칸을 비워 두고 나머지로 돌린다.
- `--red` 는 판정에만 쓴다. 어긋남, 없음, 실패다. 맞음을 초록으로 표시하지 않는다. 맞음은 강조색이다.
- 본문 글자는 `--ink` 검정이다. 회색 본문을 만들지 않는다. `--mute` 는 부연과 캡션 전용이다.

## 골격

```text
.viewport            뷰포트 전체. 레터박스의 색을 갖는다 (.dark · .blue 로 바뀐다)
  .stage             1920 x 1080 고정. transform: scale() 로만 화면에 맞춘다
    .s               쪽 하나. padding: var(--pad), flex column, gap: var(--gap)
      .head          제목 덩이
      .main          본체. flex: 1 로 남는 세로를 다 먹는다
      .foot          쪽 번호. 오른쪽 아래 구석에 자동으로 붙는다
```

- 설계 크기는 1920 x 1080 **고정**이다. 배율은 `s = min(vw / 1920, vh / 1080)` 하나이고 설계 폭을 늘리지 않는다.
- 화면비가 맞지 않으면 레터박스가 남는다. 감추지 않고 그 쪽의 캔버스 색으로 둔다.
- 쪽 번호는 `.foot` 이 자동으로 붙인다. 표지처럼 번호가 없어야 하는 쪽만 `data-nofoot` 을 단다.
- 본체를 좌우로 가를 때는 `.cols`(반반) 또는 `.cols.side`(1fr + 560) 다. 임의 비율을 만들지 않는다.
- 같은 자리에 여러 상태를 겹칠 때는 `.stack` 이다. 자식이 전부 `grid-area: 1 / 1` 에 놓여 자리가 흔들리지 않는다.

## 단계

한 쪽 안에서 무엇이 언제 켜지는지다.

- `data-steps="n"` 이 그 쪽의 단계 수다. `data-step="k"` 가 붙은 요소는 k 번째 단계에서 켜진다.
- 켜짐은 불투명도와 흐림만 바꾼다. 자리는 처음부터 끝까지 그대로다. 꺼진 요소도 자리를 차지한다.
- `data-out="k"` 는 반대다. k 단계에서 꺼진다.
- 묶음에 `data-stagger="ms"` 를 달면 그 안의 같은 단계 요소가 차례로 켜진다. 상한은 60ms 다.
- 큰 덩이(판, 카드, 층)는 `--blur-lg` 로 덜 흐리게 켠다. 크게 흐리면 덩이가 번진다.
- 자바스크립트가 필요한 쪽은 `HOOK[쪽id] = { step(k), reset() }` 을 등록한다. `step` 은 단계가 바뀔 때, `reset` 은 그 쪽을 떠난 뒤에 불린다.
- 장을 넘길 때 같은 물건이 자리를 옮기면 두 쪽의 요소에 같은 `data-morph` 이름을 단다. 옛 자리에서 새 자리로 미끄러진다.

## 넘김

발표 전체에서 하나다. 청중도 발표자도 한 번만 배운다.

| 조작 | 결과 |
|---|---|
| Space, 화면 클릭 | 이 쪽의 다음 단계. 단계가 끝나면 다음 쪽 |
| → , PageDown | 다음 쪽 |
| ← , PageUp, Shift+클릭 | 앞 쪽 |
| Home / End | 처음 / 끝 |

- 화면 클릭은 Space 와 같다. 고르는 부품(알약, 항목 줄, 카드)과 링크는 자기 동작만 하고 쪽을 넘기지 않는다. 그 부품의 클릭 핸들러가 `e.stopPropagation()` 으로 전파를 낚는다.
- 자기 할 일이 끝난 부품은 전파를 낚지 않는다. 한 번 눌러 열린 카드를 다시 누르면 보통 클릭처럼 다음으로 간다. 죽은 영역을 남기지 않는다.
- 글자를 끌어 고르는 중이거나 선택 영역이 있으면 넘기지 않는다. 더블클릭은 한 번으로 센다.
- 조작 뒤에 초점을 놓는다. 방향키가 즉시 듣는다.
- `window.finish()` 가 그 쪽의 완료 상태다. 키에 매지 않고 함수로 둔다. 검증 게이트가 이것을 부른다.

## 모션

- 등장은 길고 느리게 멎는다(`--t-in` 180, `--t-in-lg` 220). 퇴장은 짧다(`--t-out` 100). 상태 변화는 고르다(`--t-state` 160).
- 곡선은 `--ease-out` 하나가 기본이다. 자리를 옮기는 것만 `--ease-move`, 쪽 넘김과 흐림은 `--ease-smooth` 다.
- 등장과 상태 변화에 `transform` 을 쓰지 않는다. 불투명도와 흐림과 색과 폭만 바꾼다.
- `transform` 의 예외는 둘이다. 스테이지를 뷰포트에 맞추는 배율과, 쪽 사이에서 같은 물건을 잇는 `data-morph` 다.
- 내용만 바뀌는 자리는 사라졌다 나타나지 않는다. 옛 모습을 그 자리에 겹쳐 두고 새 모습이 올라오는 동안 옛 모습이 내려간다(`swapText`).
- `prefers-reduced-motion: reduce` 에서는 전환 시간과 흐림이 전부 0 이 된다. 상태 변화 자체는 남는다.

## 프로젝트가 덮는 것

강조색 계열 셋뿐이다. 그 밖의 값을 프로젝트마다 다르게 두지 않는다.

- `--acc` — 그 프로젝트의 강조색
- `--acc-soft` — 그 강조색의 옅은 면
- `--n1`~`--n7` — 그 강조색의 명도 계단 일곱 단

프로젝트의 `tokens.css` 는 이 셋만 담는다. 나머지를 다시 적으면 루트 값이 바뀔 때 따라오지 않는다.

| 프로젝트 | `--acc` |
|---|---|
| `ax-education` | `#1428A0` 네이비 |
| `demos/prompt-caching` | `#533AFD` 보라 |
| `demos/subagents` | `#533AFD` 보라 |

## 종전 체계와 달라진 것

`ax-education/디자인시스템.md` 가 적고 있던 체계와 무엇이 달라졌는지다. 옛 값으로 만든 덱을 읽을 때 대조한다.

| 항목 | 종전 | 지금 |
|---|---|---|
| 면 이름 | `--surface` / `--bg` / `--sunken` | `--white` / `--paper` / `--paper-2` / `--paper-3` |
| 본문 크기 | 24 | 32 |
| 글자 사다리 | 20 / 24 / 32 / 64 / 88 / 128 / 200 | 26 / 32 / 40 / 48 / 64 / 96 / 120 / 220 |
| radius | 다섯 값 (56 / 32 / 24 / 14 / 999) | 세 값 (16 / 12 / 4) |
| 그림자 | 금지 | `--shadow` 한 값. 접지선 전용 |
| 설계 폭 | 1920~2560 가변 | 1920 고정 |
| 격자 간격 | 24 하나 | 간격 사다리 일곱 값 |
| 서체 | Pretendard + SamsungSharpSans | Pretendard 하나 |
| 강조색 | 프로젝트마다 다른 체계 | 이름은 하나(`--acc`), 값만 프로젝트별 |
| 단계 구현 | `PAGE[n] = { step, finish, reset }` | `data-step` + `HOOK[id] = { step, reset }` |
| 화면 클릭 | 넘기지 않는다 | Space 와 같다 |
| 완료 상태 | End 키 | `window.finish()` |
