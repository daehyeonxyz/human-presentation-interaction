# 조작 문법과 판 기록

한 쪽의 조작을 어떤 문법으로 짤지 고르고, 앞서 실물로 만든 판이 무엇이었는지 확인하러 여는 문서다. 레이아웃 패턴은 [layout-taxonomy.md](layout-taxonomy.md) 가 원본 택소노미로 지목한다.

여기 남은 것은 실물 제작으로 검증된 항목뿐이다. 절대 좌표 규격은 벤토 채움 계약과 어긋나므로 옮기지 않았다.

## 조작 문법

한 쪽에 걸리는 조작의 종류다. 한 쪽에 조작 문법을 두 가지까지만 두고 세 가지를 넘기지 않는다. 세 가지를 넘으면 청중이 매번 새 조작을 익혀야 한다.

| ID | 문법 | 무엇을 하는가 | 정지 상태의 표시 | 대표 부품 |
|---|---|---|---|---|
| `g-none` | 없음 | 조작하지 않는다 | 없음 | 표지, 정지 구성 |
| `g-pick` | 고르기 | 여럿 가운데 하나를 눌러 그것을 연다 | 흰 면과 커서와 호버 링 | 누르는 항목 |
| `g-advance` | 전진 | 버튼 하나로 다음 단계로 이동한다 | 버튼과 단계 점 | 버튼, 단계 점 |
| `g-toggle` | 뒤집기 | 같은 조작으로 켜고 끈다 | 버튼 이름이 현재 상태의 반대 | 버튼, 접기 판 |
| `g-point` | 가리키기 | 한쪽에 마우스를 올리면 다른 쪽의 대응이 표시된다 | 커서와 호버 링 | 범례, 인스펙터 |
| `g-pin` | 고정하기 | 가리킨 것을 눌러 표시를 유지한다 | 고른 것의 링 | 범례, 인스펙터 |
| `g-drag` | 끌기 | 핸들을 잡아 값이나 경계를 옮긴다 | 핸들과 트랙 | 슬라이더, 비교 핸들 |
| `g-input` | 입력 | 글자나 값을 넣고 결과를 받는다 | 요청이 담긴 입력 칸 | 입력 바, 전송 버튼 |

- `g-point` 와 `g-pin` 은 짝이다. 호버 미리보기만 두면 발표자가 마우스를 잡은 채로 말해야 하므로 무대에서 성립하지 않는다. 미리보기를 두면 고정도 함께 둔다.
- `g-input` 은 발표자가 실제로 타이핑하는 것이 아니라 눌렀을 때 요청이 이력으로 올라가는 재현을 뜻한다. 무대에서 키보드를 치는 조작은 두지 않는다.
- 조작이 그 쪽의 주장을 증명하지 않으면 조작을 붙이지 않고 등급 0 정지로 내린다. 클릭으로 감췄다 여는 것만으로는 논증에 기여하지 않는다.

## 메시지의 논리 구조

그 쪽의 핵심 메시지 한 줄이 어떤 꼴인지다. 구성 단계에서 메시지를 먼저 쓰고 여기서 구조를 판정한다.

| ID | 구조 | 메시지의 꼴 | 요구하는 화면 |
|---|---|---|---|
| `m-list` | 나열 | A 와 B 와 C 가 있다 | 같은 규격의 항목 여럿 |
| `m-order` | 순서 | A 다음에 B 가 온다 | 세 가지 무게로 갈리는 항목 |
| `m-contrast` | 대비 | A 와 B 가 다르다 | 같은 크기 둘이 같은 높이에 나란함 |
| `m-process` | 과정 | A 를 되풀이하면 B 가 된다 | 단계마다 상태가 바뀌는 장치 하나 |
| `m-accumulate` | 누적 | 조작할수록 쌓인다 | 조작 창과 관찰 창의 분리 |
| `m-decompose` | 분해 | A 는 실은 B 와 C 로 이루어져 있다 | 접힘과 펼침, 또는 조각 나누기 |
| `m-relation` | 관계 | A 의 이것이 B 의 저것이다 | 두 표현이 서로 대응하는 배선 |
| `m-distribution` | 분포 | 값이 이렇게 나뉘어 있다 | 게이지와 확률 목록 |
| `m-declare` | 선언 | 오늘 확인한 것은 이것이다 | 큰 문장 하나 |

## 복잡도 등급

`SKILL.md` 의 복잡도 사다리 0~3 등급이다. 등급은 메시지가 정하고 조작 문법은 그 등급을 담을 수 있는지로 걸러진다.

| 등급 | 형태 | 조작 문법 | 최소 요건 |
|---|---|---|---|
| 0 정적 | 다이어그램, 표, 정지 구성 | `g-none` | 정지 상태로 구조가 다 보인다 |
| 1 공개 | 호버 공개, 클릭 팝업, 토글 | `g-pick` · `g-point` · `g-toggle` | 상세를 감춰 인지 부하를 줄인다 |
| 2 단계 | 단계 진행, 상태 전환, 비교 | `g-advance` · `g-drag` · `g-pin` | 조작 전후의 화면이 다르고 그 차이가 메시지다 |
| 3 시뮬레이션 | 재생 가능한 모델 + 탐색 + 인스펙터 | `g-advance` + `g-point` + `g-pin`, `g-input` | 상태와 시퀀스와 상세가 분리돼 있고 되돌림이 있다 |

- 등급을 올리려면 근거 한 줄을 채널 배분 표에 적는다.
- 등급 3 은 한 발표에 하나까지다.
- 등급 2 이상은 조작 관찰 증거를 `qa/` 에 남겨야 완료로 친다.

## 조합 규칙

- 한 쪽의 조작 문법은 두 가지까지다.
- 같은 조작 문법이 이어지는 쪽을 붙여 배치한다. 조작 문법이 매 쪽 바뀌면 그 발표에서 청중이 익히는 것이 조작뿐이 된다.
- 가리키기는 다른 문법 위에 층으로 얹을 수 있다. 원래 문법이 이미 둘이면 얹지 않는다.
- 청중이 이미 아는 화면(채팅 재현 등)이 있으면 그것을 여러 쪽에서 되풀이해 쓴다. 되풀이가 학습 비용을 없앤다.
- 맺음은 그 쪽 장치의 마지막 행이다. 정지 상태에도 자리가 잡혀 있어서 켜질 때 위의 것이 밀리지 않는다.

## 안정성 가드레일

판 선택과 무관하게 항상 적용한다. 선택이 아니라 기본이다.

### overflow-containment · 넘침 제어

- 모든 쪽의 `scrollHeight` 가 설계 높이 이하이고 `scrollWidth` 가 그때의 스테이지 폭 이하다.
- 조작을 다 마친 상태에서도 여백 80 을 침범하지 않는다. 정지 상태만 재고 통과시키지 않는다.
- 여백 80 은 kicker 와 쪽 번호만이 아니라 보이는 요소 전수로 잰다.
- 자동 줄바꿈이 일어나지 않게 블록 폭을 최장 줄에 맞춘다. 줄 나눔은 원고가 정하고 화면 폭에 맡기지 않는다.
- `word-break: keep-all` 을 기본으로 걸고 flex 자식에 `min-width: 0` 을 준다.
- **다른 자리로 이동하는 요소는 경로를 연다.** 이동이 지나갈 조상에 `overflow: hidden` 이 걸려 있으면 요소가 칸을 벗어나는 순간 잘린다. 넘침을 막는 규칙과 이동을 보이는 규칙이 같은 조상에서 부딪히면 이동 쪽이 이긴다. 잘림은 관찰로만 잡히고 정지 캡처에는 나오지 않는다.

### reserved-slot · 칸 예약

- 조작 뒤에 표시될 칸을 정지 상태에 미리 잡는다. 높이만 잡고 면과 글자는 함께 켜진다.
- 미리 우물 면을 칠해 두지 않는다. 글자 없는 빈 상자가 화면에 남는다.
- 아래로 늘어나는 칸에만 적용한다. 아래 끝을 고정하고 위쪽으로 늘어나는 판은 이 규칙에서 빠진다.

### growth-direction · 늘어나는 방향

- 판마다 늘어나는 방향을 하나로 정하고 문서에 기록한다.
- 아래로 늘어나면 아래에 있는 것이 밀린다. 밀려도 되는 것은 그 판 안의 내용뿐이다.
- 위쪽으로 늘어나면 아무것도 밀지 않는다. 접힘과 펼침의 높이 차이가 메시지인 판은 반드시 이쪽이다.
- 높이에 전환을 걸지 않는다. 높이가 애니메이션되면 그 사이 프레임에서 아래 내용이 흔들린다.

### no-position-shift · 위치 이동 금지

- 등장과 상태 변화에 `transform` 을 쓰지 않는다. 불투명도와 흐림과 색과 폭만 바꾼다.
- `transform` 의 예외는 둘뿐이다. 스테이지를 뷰포트에 맞추는 배율과, 아래 `object-continuity` 의 자리 이어받기다. 그 둘 말고 배치에 쓰지 않는다.
- 링은 `border` 가 아니라 `box-shadow` 로 넣는다. `border` 는 1px 밀림을 만들고 그 밀림이 이동으로 보인다.
- 링은 안쪽으로 넣는다. 바깥 링은 옆 항목의 칸을 침범한다.
- `prefers-reduced-motion: reduce` 환경에서는 전환 시간 토큰이 전부 0 이 된다.

### keyboard-recovery · 키보드 복구

- 조작 뒤에 방향키로 쪽을 넘기는 것이 즉시 된다. 조작 직후 `document.activeElement` 를 해제한다.
- 입력 칸이나 슬라이더가 초점을 가진 동안에만 좌우 키를 가로챈다. 초점을 잃으면 즉시 넘김으로 돌려준다.
- 키 배치를 발표 전체에서 하나로 고정한다. 좌우는 쪽 넘김, Space 는 그 쪽의 다음 조작, End 는 그 쪽의 완료 상태다.
- 완료된 쪽에서 Space 는 그 쪽을 처음 상태로 되돌린다. 완료 감지 표시는 맺음 요소의 `on` 이고 이 계약을 켠 덱은 `window.SPACE_RESTART` 를 참으로 둔다.
- 누르는 요소는 Tab 으로 초점을 받고 Enter 로 눌린다. 초점 링은 두지 않는다.

### hover-response · 호버 반응

- 커서가 pointer 인 요소는 전부 호버에서 보이는 반응을 낸다. 반응 없는 pointer 커서는 커서가 거짓 신호를 보내는 상태이고 반려 사유다.
- 반응의 형태는 세 가지 가운데 하나 이상이다. 안쪽 호버 링, 면 밝기 변화, 대응 요소의 교차 강조다.
- **반응은 한 가지로 끝낸다.** 들뜸과 그림자와 색을 함께 걸면 무엇이 눌리는지 알리는 일에 견줘 과하다. 목록처럼 항목이 많은 판은 글자색 하나만 바꾼다.
- 호버 반응의 전환 시간은 `--dur-base` 하나를 쓴다.
- `g-point` 요소의 호버는 반드시 대응 요소에도 표시를 켠다. 한쪽에만 걸린 교차 강조는 미완성이다.
- 호버로만 닿는 정보를 만들지 않는다. 호버 미리보기가 있는 곳에는 클릭 고정이 함께 있다.
- 검사는 자동 게이트로 한다. pointer 요소 전수에 mouseover 를 걸어 computed style 변화가 없는 것을 실패로 센다.

### static-fallback · 정적 폴백

- 모든 판에 완료 상태를 즉시 표시하는 함수를 둔다. 시간차 예약을 되풀이하는 방식으로 만들지 않는다.
- 완료 상태는 되풀이해 불러도 같은 화면이어야 한다.
- 완료 상태를 만들 때 창 안 스크롤은 부드러움을 끄고 단번에 아래 끝으로 보낸다.

### stage-reset · 쪽 되돌림

- 다른 쪽에 갔다 오면 그 쪽의 조작은 처음 상태다.
- 되돌림은 쪽을 떠난 뒤에 실행한다. 떠나는 전환이 보이는 동안 되돌리면 청중이 되돌아가는 화면을 본다.
- 예약된 타이머를 쪽마다 모아 두고 떠날 때 함께 제거한다.
- **돌아오면 예약된 되돌림을 취소한다.** 떠나는 전환이 보이는 동안 되돌리지 않으려고 되돌림을 지연시키면, 그 지연 안에 같은 쪽으로 돌아왔을 때 되돌림이 방금 들어온 쪽을 초기화한다. 쪽을 활성화할 때 그 쪽에 걸린 되돌림 예약을 먼저 취소한다. 앞뒤로 빠르게 오갈 때만 나타나므로 한 방향으로만 넘겨 보면 잡히지 않는다.

### content-swap · 내용 교체

- 같은 자리의 내용이 바뀔 때 **사라졌다 나타나지 않는다.** 옛 모습을 그 자리에 겹쳐 두고, 새 모습이 올라오는 동안 옛 모습이 내려간다.
- 글자든 숫자든 카드든 같다. 숫자가 바뀌는 자리는 값을 굴리고, 문장이 바뀌는 자리는 겹쳐 교차한다.
- 빈 화면이 한 프레임이라도 보이면 청중은 그것을 쪽이 바뀐 것으로 읽는다.
- 나중에 켜지는 것은 흐림에서 떠오른다. 불투명도와 흐림만 쓰고 자리는 처음부터 그대로다 (`reserved-slot` 과 짝이다).

### object-continuity · 물건 연속

- 앞 쪽에 있던 물건이 뒤 쪽에도 있으면 **같은 자리에서 이어받는다.** 쪽이 바뀌었다고 그 물건이 새로 등장하면 청중은 같은 물건인 줄 모른다.
- 자리를 잇는 방법은 둘이다. 뒤 쪽 진입 좌표를 앞 쪽의 마지막 좌표로 고정하거나, 앞 쪽의 좌표를 재서 뒤 쪽 물건을 그 자리에서 제자리로 옮긴다(FLIP). 후자가 `no-position-shift` 의 두 번째 예외다.
- 이어받는 물건은 쪽마다 규격이 같아야 한다. 폭이나 모서리가 달라지면 이어붙는 대신 튄다.
- 쪽을 잇는 물건을 문서에 적는다. 무엇이 몇 쪽에서 몇 쪽으로 이어지는지가 그 발표의 뼈대다.

### beat-parity · 박자 일치

- **Space 한 번이 대본의 한 마디다.** 대본을 먼저 쓰고 그 마디 수만큼 단계를 만든다. 단계를 먼저 만들고 대사를 나눠 붙이지 않는다.
- 한 번에 두 현상이 일어나면 대본이 그 사이에서 끊긴다. 한 마디 안에서 함께 일어나야 하는 것만 한 단계에 묶는다.
- 단계 훅은 단계마다 다시 불린다. 한 번만 일어나야 하는 애니메이션은 실행 여부를 기억해 재실행을 막는다. 막지 않으면 다음 단계에서 끝난 동작이 다시 돈다.
- 대본이 바뀌면 단계도 바뀐다. 대본과 단계 수가 어긋난 쪽은 반려다.

### entry-state · 진입 상태

- 여럿 가운데 하나를 고르는 판은 **진입할 때 첫 항목이 이미 골라져 있다.** 빈 상태로 들어가면 첫 Space 가 "고르기"에 쓰이고 대본의 첫 마디가 낭비된다.
- 골라져 있는 것은 목록의 처음이다. 발표 순서가 곧 목록 순서이므로 처음이 아닌 것을 골라 두면 목록의 방향이 흐려진다.
- 인스펙터와 곁 설명도 그 첫 항목의 것을 보인다. 비우면 창 아래가 빈 상자로 남는다.

### motion-parity · 속도 일치

- 같은 성격의 변화는 **같은 시간 토큰**을 쓴다. 한 쪽 안에서 글자 교체가 240ms 인데 점 이동이 520ms 이면 둘이 따로 논다.
- 성격은 셋으로 가른다. 켜짐과 꺼짐, 내용 교체, 자리 이동이다. 셋마다 토큰 하나씩 두고 판마다 새로 정하지 않는다.
- 되튀는 이징을 쓰지 않는다. 무대에서 되튐은 장식으로 읽힌다.
- **이동과 페이드를 같은 구간에 걸지 않는다.** 함께 걸면 첫 프레임에서 투명해져 이동이 보이지 않는다. 불투명도는 이동의 뒤쪽 구간에만 건다.
- 쪽 넘김도 전환이다. 즉시 끊으면 앞뒤가 다른 발표처럼 보인다. 나가는 쪽이 짧게 흐려지며 물러나고 들어오는 쪽이 그 아래에서 이미 자리를 잡고 있다.

### state-color · 상태 색

- 개념이 갈리는 곳에서 **색이 그 갈림을 나른다.** 같은 개념은 발표 전체에서 같은 색이고, 대립하는 개념은 명도가 아니라 색상으로 가른다.
- 한 단계에서 바뀐 것만 강조색을 받고 다음 단계에서 원래 색으로 돌아온다. 강조가 쌓이면 무엇이 방금 바뀌었는지 알 수 없다.
- 초점을 옮기는 판은 초점 밖을 흐린다. 밝기만 낮추면 여전히 읽히고 초점이 서지 않는다.
- 강조 면은 옅게 깔고 글자가 면보다 앞에 온다. 면이 진해 글자를 덮으면 강조가 가림이 된다.

### stacking-discipline · 겹침 규율

- 겹치는 것은 팝업 카드와 시작 전 덮개 두 가지뿐이다. z-index 를 두 단계로 고정한다.
- 팝업이 무대의 무엇을 덮고 무엇을 덮지 않는지 재서 문서에 기록한다.

### self-contained · 자기완결

- 산출물은 외부 파일에 의존하지 않는 단일 HTML 이다. 웹폰트와 이미지를 base64 로 인라인한다.
- 무대에서 네트워크가 없어도 서체가 폴백으로 내려가지 않아야 한다. 서체가 바뀌면 줄 나눔 규격이 전부 무효가 된다.
- 서체는 실제 쓰는 글자로 서브셋해 넣는다. 서브셋 스크립트를 `qa/` 에 남겨 원고가 바뀌면 다시 생성한다.

## 실물로 검증된 판 기록

실제로 만들어 게이트를 통과시킨 구성만 적는다. 다음 발표에서 같은 논리 구조를 만나면 여기서 먼저 찾는다.

| 조작 문법 | 논리 구조 | 등급 | 무대 구성 | 실물 |
|---|---|---|---|---|
| `g-none` | `m-list` | 0 | 표제가 왼쪽 네 칸을 세로로 쓰고 항목 셋이 오른쪽 여덟 칸을 나눈다 | 덱 1호 2쪽, 덱 2호 2쪽 |
| `g-advance` + `g-point`·`g-pin` | `m-process` | 2 | 장치가 아홉 칸을 쓰고 곁 문장이 오른쪽 세 칸에 선다. 조작부와 맺음이 장치의 마지막 두 행이다 | 덱 1호 3쪽 |
| `g-advance` + `g-point`·`g-pin` | `m-accumulate` | 3 | 조작 창(여덟 칸)과 관찰 창(네 칸). 관찰 창 맨 위가 상시 표시 층이고 맨 아래가 인스펙터다 | 덱 1호 4쪽 |
| `g-point`·`g-pin` | `m-relation` | 2 | 대응하는 둘을 같은 행에 나란히 놓는다. 대응이 배치로 먼저 읽히고 가리키기가 그것을 짚는다 | 덱 1호 5쪽 |
| `g-advance` | `m-declare` | 1 | 선언이 왼쪽 다섯 칸을 세로로 쓰고 되짚기 항목이 오른쪽 일곱 칸을 나눈다 | 덱 1호 6쪽, 덱 2호 6쪽 |
| `g-drag` + `g-point`·`g-pin` | `m-contrast` | 2 | 같은 구조 두 벌을 같은 좌표에 겹치고 세로 경계가 어느 쪽을 보일지 정한다 | 덱 2호 3쪽 |
| `g-input` + `g-point`·`g-pin` | `m-process` | 3 | 조작 창(여덟 칸)에서 요청을 보내고 관찰 창(네 칸)이 그 요청의 구성을 보인다. 이력은 아래 끝을 고정하고 위로 쌓인다 | 덱 2호 4쪽 |
| `g-drag` | `m-distribution` | 2 | 값과 게이지와 슬라이더가 한 장치에 세로로 쌓이고 곁 문장이 오른쪽 네 칸에 선다 | 덱 2호 5쪽 |
| `g-advance` | `m-process` | 2 | 왼쪽 제품 재현(채팅), 오른쪽 장부. 단계마다 장부의 한 행이 바뀌고 바뀐 행만 색이 든다. 대본의 마디와 단계가 1대1 | AX101 8·9쪽 |
| `g-pick` + `g-advance` | `m-order` | 1 | 왼쪽 도형이 등급마다 커지고 오른쪽 단추 줄과 설명이 그 등급을 받는다. 진입 시 첫 단추가 골라져 있다 | AX101 12쪽 |
| `g-advance` | `m-decompose` | 2 | 상자 셋이 아래 카드의 세 줄 자리로 날아가 사라지고 상자 영역이 접히며 카드가 가운데로 올라온다. 다음 쪽이 그 카드를 같은 자리에서 이어받는다 | AX101 35·36쪽 |
| `g-advance` | `m-accumulate` | 2 | 위에 컨텍스트 막대 한 줄, 아래에 실물 조각들. 단계마다 막대의 한 칸과 그 칸에 해당하는 조각이 짝으로 켜진다 | AX101 41쪽 |

- 표지는 조작이 없고 큰 글자가 본체 행을 다 쓴다. 아래 끝 줄이 바닥을 잡는다.
- 등급 0 정지 쪽에서 항목이 셋이면 표제를 위 띠에 두지 않고 왼쪽으로 세운다. 위 띠에 두면 항목 칸이 세로로 커져 글자가 칸 안에서 흩어진다.
- 관찰 창의 인스펙터는 고른 것이 없을 때도 첫 항목을 보인다. 비우면 창 아래가 빈 상자로 남는다.
- 장치 안에서 조작부와 맺음은 한 덩어리로 묶는다. 따로 두면 `space-between` 이 둘 사이를 크게 벌린다.
- 제품 재현을 쓰는 판은 재현을 왼쪽에, 그 재현을 읽는 장치(장부·막대·설명)를 오른쪽에 둔다. 청중이 왼쪽에서 무슨 일이 일어났는지 보고 오른쪽에서 그 뜻을 받는다. 반대로 두면 뜻을 먼저 읽고 근거를 나중에 본다.
- 여러 쪽에 걸쳐 되풀이되는 제품 재현은 첫 쪽에서 정한 폭을 끝까지 쓴다. 쪽마다 곁 장치의 분량이 달라도 재현 폭은 고정이고, 남는 자리는 곁 장치가 가져간다.
