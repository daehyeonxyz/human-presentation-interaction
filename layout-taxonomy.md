# 인터랙티브 프레젠테이션 레이아웃 택소노미

## 개요

이 문서는 한 쪽의 화면 구조를 고를 때 쓰는 분류 체계다. `SKILL.md` 의 3단계 구성과 5단계 채널 배분과 7단계 디자인 시스템이 이 문서에서 패턴 ID 를 지목한다.

- 대상은 발표 무대에 띄우는 마진 보존 가변 폭 스테이지(세로 1080 기준) 한 쪽이다.
  - 스크롤되는 웹 페이지가 아니다. 한 쪽은 한 화면에서 끝나고 넘김으로만 이어진다.
  - 전역 `~/.claude/skills/layout-taxonomy/taxonomy.md` 는 스크롤 웹 UX 의 분류다. 그 문서의 패턴을 그대로 옮기지 않고 무대에 성립하는 것만 번안한다.

- 판은 내용의 종류가 아니라 조작의 모양으로 나뉜다.
  - 같은 내용이라도 조작이 다르면 다른 판이고, 다른 내용이라도 조작이 같으면 같은 판이다.
  - 이 원칙은 ax-education 일곱 판으로 92쪽을 구현하면서 검증됐다.

- 스테이지는 마진 보존 가변 폭이다.
  - 세로는 1080 기준 절대 좌표이고 배율은 뷰포트 높이에 맞춘다.
  - 가로는 네 변 여백 80 을 고정한 채 무대 폭이 뷰포트를 채운다. 폭의 하한은 1920 이고 상한은 2560 이다.
  - 하한 아래의 좁은 화면에서는 가로 기준으로 전체를 축소한다.
  - 이 문서의 좌표 표는 폭 1920 기준의 기본값이다. 폭이 늘어나면 왼쪽 정렬 칸은 x 를 유지하고 오른쪽 정렬 칸은 오른쪽 여백 80 을 유지하며 열 폭은 열 격자의 나눔 비율대로 늘어난다.
  - 프로젝트의 `tokens.css` 가 이 값을 변수로 갖고, 이 문서는 그 변수의 기본값을 정한다.
  - 프로젝트가 다른 값을 쓰기로 하면 `tokens.css` 가 단일 원본이고 이 문서는 그 근거만 기록한다.

## 분류 축

판은 두 축의 교차로 결정된다. 가로축은 청중과 발표자가 화면에 무엇을 하는가이고, 세로축은 그 쪽의 메시지가 어떤 논리 구조를 가지는가다.

### 조작 문법

한 쪽에 걸리는 조작의 종류다. 한 쪽에 조작 문법을 두 가지까지만 두고 세 가지를 넘기지 않는다. 세 가지를 넘으면 청중이 매번 새 조작을 익혀야 한다.

| ID | 문법 | 무엇을 하는가 | 정지 상태의 표시 | 대표 부품 |
|---|---|---|---|---|
| `g-none` | 없음 | 조작하지 않는다 | 없음 | 표지 |
| `g-pick` | 고르기 | 여럿 가운데 하나를 눌러 그것을 연다 | 흰 면과 커서와 호버 링 | 누르는 항목, 우물 트레이 |
| `g-advance` | 전진 | 버튼 하나로 다음 단계로 이동한다 | 버튼과 단계 점 | 버튼, 단계 점 |
| `g-toggle` | 뒤집기 | 같은 조작으로 켜고 끈다 | 버튼 이름이 현재 상태의 반대 | 버튼, 접기 판 |
| `g-point` | 가리키기 | 한쪽에 마우스를 올리면 다른 쪽의 대응이 표시된다 | 커서와 호버 링 | 범례, 말풍선, 각주 줄 |
| `g-pin` | 고정하기 | 가리킨 것을 눌러 표시를 유지한다 | 고른 것의 링 | 범례, 인스펙터 |
| `g-drag` | 끌기 | 핸들을 잡아 값이나 경계를 옮긴다 | 핸들과 트랙 | 슬라이더, 비교 핸들 |
| `g-input` | 입력 | 글자나 값을 넣고 결과를 받는다 | 커서가 깜빡이는 입력 칸 | 입력 바, 전송 버튼 |

- `g-point` 와 `g-pin` 은 짝이다. 호버 미리보기만 두면 발표자가 마우스를 잡은 채로 말해야 하므로 무대에서 성립하지 않는다. 미리보기를 두면 고정도 함께 둔다.
- `g-input` 은 발표자가 실제로 타이핑하는 것이 아니라 눌렀을 때 글자가 입력 칸에 차오르는 재현을 뜻한다. 무대에서 키보드를 치는 조작은 두지 않는다.

### 메시지의 논리 구조

그 쪽의 핵심 메시지 한 줄이 어떤 꼴인지다. 구성 단계에서 메시지를 먼저 쓰고 여기서 구조를 판정한다.

| ID | 구조 | 메시지의 꼴 | 요구하는 화면 |
|---|---|---|---|
| `m-list` | 나열 | A 와 B 와 C 가 있다 | 같은 규격의 항목 여럿 |
| `m-order` | 순서 | A 다음에 B 가 온다 | 세 가지 무게로 갈리는 항목 |
| `m-contrast` | 대비 | A 와 B 가 다르다 | 같은 크기 둘이 같은 높이에 나란함 |
| `m-process` | 과정 | A 를 되풀이하면 B 가 된다 | 단계마다 상태가 바뀌는 장치 하나 |
| `m-accumulate` | 누적 | 조작할수록 쌓인다 | 조작 창과 관찰 창의 분리 |
| `m-decompose` | 분해 | A 는 실은 B 와 C 로 이루어져 있다 | 접힘과 펼침, 또는 조각 나누기 |
| `m-relation` | 관계 | A 의 이것이 B 의 저것이다 | 두 표현이 서로 대응하는 배선 |
| `m-distribution` | 분포 | 값이 이렇게 나뉘어 있다 | 게이지와 확률 목록 |
| `m-declare` | 선언 | 오늘 확인한 것은 이것이다 | 큰 문장 하나 |

### 교차표

가로가 조작 문법이고 세로가 논리 구조다. 칸에 담긴 것이 그 교차의 판 ID 다. 빈 칸은 그 교차를 쓸 일이 없다는 뜻이 아니라 아직 검증된 판이 없다는 뜻이고, 필요하면 가장 가까운 판의 변형으로 만들고 그 사실을 명시한다.

| | `g-pick` | `g-advance` | `g-toggle` | `g-point`·`g-pin` | `g-drag` | `g-input` |
|---|---|---|---|---|---|---|
| `m-list` | `pick-list` | | | `chapter-cover` | | |
| `m-order` | `pick-list` | `accumulate-recap` | | | | |
| `m-contrast` | `pick-contrast` | | `pick-contrast` | | `drag-compare` | |
| `m-process` | | `advance-machine` | `advance-machine` | | | `input-sandbox` |
| `m-accumulate` | | `twin-window` | | `twin-window` | | `twin-window` |
| `m-decompose` | `inspect-annotated` | `advance-split` | `toggle-reveal` | | | |
| `m-relation` | | | | `point-map` | | |
| `m-distribution` | | `simulate-console` | | `simulate-console` | `drag-threshold` | |
| `m-declare` | | `accumulate-recap` | | | | |

## 무대 골격

### 세 구역

모든 판은 세 구역으로 이루어진다. 판이 다른 것은 이 셋의 배치가 다른 것뿐이다.

- 무대: 조작 대상이 놓이는 곳이다. 화면에서 가장 큰 면적을 차지한다.
- 조작부: 누르거나 끄는 부품이 놓이는 곳이다.
- 결과부: 조작으로 달라지는 것이 표시되는 곳이다.

세 구역의 관계는 세 가지 배치 가운데 하나로 정해진다.

- 겹침: 조작부와 결과부가 무대 안에 있다. 항목을 눌러 그 항목이 열리는 판이 여기 해당한다.
- 안쪽: 조작부가 장치 안에 있고 결과부가 그 장치의 다른 칸이다. 장치 하나를 버튼으로 진행시키는 판이 여기 해당한다.
- 좌우: 왼쪽이 조작 창이고 오른쪽이 관찰 창이다. 조작한 것이 다른 창에 쌓이는 판이 여기 해당한다.

### 기준 좌표

여기 적힌 y 값은 부제가 있는 쪽의 기본값이다. 부제가 없는 쪽은 무대가 240 에서 시작하고 높이가 584 이며 아래 끝은 824 로 같다.

| 칸 | x | y | 크기 | 규격 |
|---|---|---|---|---|
| 머리 행 | 80 | 80 | 1760x24 | 왼쪽 끝 kicker, 오른쪽 끝 쪽 번호 |
| 제목 | 80 | 120 | 1160x72 | 한 쪽에 하나. 폭을 1160 으로 제한해 짧은 제목을 강제한다 |
| 영문 부제 | 80 | 208 | 28 높이 | 라틴 전용. 없어도 된다 |
| 머리 오른쪽 칸 | 1280 | 128 | 560 | 그 쪽을 여는 리드 문장. 무대 높이를 차지하지 않는다 |
| 무대 | 80 | 284 | 1760x540 | 아래 끝 824 고정 |
| 맺음 밴드 | 80 | 아래 끝 1000 | 폭 max-content | 조작이 끝나야 켜진다 |

- 네 변 여백은 모두 80 이다. 맨 아래 글자의 아래 끝은 y 1000 을 넘지 않는다.
- 무대 아래 끝 824 는 판이 바뀌어도 달라지지 않는다. 824 와 1000 사이 176 은 맺음 밴드가 확정으로 쓴다.
- 표지 계열만 네 변 여백 60 을 쓴다.

### 열 격자

무대 폭(내용 폭 `--content-w`)을 나누는 방식은 다섯 가지뿐이다. 그 밖의 폭을 쓰지 않는다. 표의 값은 폭 1920 기준이고 폭이 늘어나면 같은 비율로 나눈다.

| 이름 | 나눔 (1920 기준) | 비율 | 쓰임 |
|---|---|---|---|
| 전폭 | 1760 | 내용 폭 전체 | 장치 하나가 무대를 다 쓸 때 |
| 넓은 짝 | 1160 + 560 | 내용 폭에서 3열 하나를 뺀 나머지 + 3열 하나 | 주열과 딸림 열의 무게가 다를 때 |
| 대등 짝 | 860 + 860 | 2 등분 | 두 창의 무게가 같을 때 |
| 3열 | 560 x 3 | 3 등분 | 같은 규격 셋을 나열할 때 |
| 4열 | 410 x 4 | 4 등분 | 같은 규격 넷을 나열할 때 |

- 열 사이 간격은 40 이다.
- 문장 한 줄의 폭 상한은 700 이고 폭이 늘어나도 늘어나지 않는다. 남는 가로는 무대와 장치가 채우지 문장이 늘어나서 채우지 않는다.
- 정렬 축은 한 쪽에 하나다. 개념을 설명하는 화면은 좌측 정렬이고, 대등한 항목을 나열하는 화면만 칸 안에서 가운데 정렬을 허용한다.

## 판 카탈로그

### 판 목록

| ID | 이름 | 조작 문법 | 논리 구조 | 등급 | 세 구역 배치 |
|---|---|---|---|---|---|
| `cover-stage` | 표지 | `g-none` | | 0 | 없음 |
| `chapter-cover` | 장 표지 | `g-point` | `m-list` | 1 | 겹침 |
| `pick-list` | 목록 무대 | `g-pick` | `m-list`·`m-order` | 1~2 | 겹침 |
| `pick-contrast` | 대비 무대 | `g-pick`·`g-toggle` | `m-contrast` | 2 | 겹침 |
| `toggle-reveal` | 펼침 무대 | `g-toggle` | `m-decompose` | 2 | 겹침 |
| `advance-machine` | 기계 무대 | `g-advance`·`g-toggle` | `m-process` | 2~3 | 안쪽 |
| `advance-split` | 쪼갬 무대 | `g-advance` | `m-decompose` | 2 | 안쪽 |
| `twin-window` | 두 창 무대 | `g-advance`·`g-point`·`g-input` | `m-accumulate` | 2~3 | 좌우 |
| `point-map` | 짚기 무대 | `g-point`·`g-pin` | `m-relation` | 1~2 | 좌우 |
| `inspect-annotated` | 주석 무대 | `g-pick`·`g-point` | `m-decompose` | 2 | 겹침 |
| `drag-compare` | 겹침 비교 | `g-drag` | `m-contrast` | 2 | 겹침 |
| `drag-threshold` | 값 조절 무대 | `g-drag` | `m-distribution` | 2~3 | 안쪽 |
| `input-sandbox` | 입력 무대 | `g-input` | `m-process` | 3 | 좌우 |
| `simulate-console` | 시뮬레이션 콘솔 | `g-advance`·`g-point`·`g-pin` | `m-process`·`m-distribution` | 3 | 좌우 |
| `accumulate-recap` | 누적 정리 | `g-advance` | `m-declare` | 1~2 | 겹침 |
| `workshop-steps` | 실습 무대 | `g-pick` | `m-order` | 2 | 좌우 |

### cover-stage · 표지

발표 전체나 한 장의 첫 쪽이다. 조작이 없는 유일한 판이다.

- 조작 문법: `g-none`
- 논리 구조: 없음
- 등급: 0
- Best For: 발표 전체의 첫 쪽, 청중이 입장하는 동안 띄워 두는 쪽
- Avoid For: 내용을 담아야 하는 쪽. 표지에 목차나 요지를 함께 두지 않는다
- Good With: `chapter-cover`
- Avoid With: `closing-band` 를 포함한 모든 본문 부품

| 칸 | 위치 | 규격 |
|---|---|---|
| 배경 | 전면 | 사진 center/cover 위에 스크림 |
| 워드마크 | (80, 60) | 글자 높이 44 |
| 오른쪽 위 영문 | 우측 1840, y 60 | 20/500 라틴 대문자, 자간 0.18em |
| 제목 | (80, 402) | 200/700 라틴, 자간 -0.02em |
| 태그라인 | (80, 634) | 24/500 |
| 오른쪽 아래 영문 | 우측 1840, 아래 60 | 20/500, 최대 3줄, 우측 정렬 |

- 캔버스가 어두우므로 레터박스 색도 어두운 값으로 바꾼다. 밝은 값으로 고정해 두면 전체화면에서 위아래에 밝은 띠가 남는다.
- kicker 와 부제와 맺음과 쪽 번호와 조작 안내를 두지 않는다.
- 배경 사진은 스테이지 1920 에서 이미 확대되므로 원본이 2560 이상이어야 한다.

### chapter-cover · 장 표지

여러 장으로 나뉜 발표에서 한 장의 첫 쪽이다. 표지와 같은 사진을 쓰되 스크림을 균일하게 덮어 사진의 대비를 낮춘다.

- 조작 문법: `g-point`
- 논리 구조: `m-list`
- 등급: 1
- Best For: 장이 셋 이상인 긴 발표, 지금 어디를 지나는지 알려야 할 때
- Avoid For: 장이 하나뿐인 발표. 표지 뒤에 장 표지가 곧바로 오면 같은 성격의 쪽이 두 번 이어진다
- Good With: `cover-stage`
- Avoid With: `accumulate-recap` (같은 발표에서 진행 위치를 두 부품이 각각 표시하게 된다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 배경 | 전면 | 사진 위에 균일 스크림 |
| 워드마크 | (80, 60) | 글자 높이 32 |
| 장 라벨 | (80, 288) | 20/700 라틴, 자간 0.06em |
| 질문 | (80, 340) 폭 1400 | 128/800, 최대 2줄 |
| 리드 | (80, 668) 폭 1160 | 24/500, 최대 2줄 |
| 장 스트립 | (80, 아래 80) 1760 | N칸, 번호 20/700 위에 축약 20/500 최대 2줄 |

- 조작은 가리키기 하나다. 스트립의 다른 장에 마우스를 올리면 그 장이 무엇을 다루는지 한 줄이 표시된다.
- 현재 장과 나머지 장을 글자 색으로 구분한다. 잇는 선이나 화살표를 그리지 않는다.
- 쪽 번호를 두지 않는다.

### pick-list · 목록 무대

항목 N개가 놓이고 각 항목이 조작 대상이며 조작 결과가 그 항목 안에 표시된다.

- 조작 문법: `g-pick`
- 논리 구조: `m-list` 또는 `m-order`
- 등급: 하나만 열리면 1, 누른 것이 다 남고 맺음이 켜지면 2
- Best For: 항목 여럿을 하나씩 눌러 확인하는 쪽, 앞의 답이 다음 항목의 전제가 되는 순서
- Avoid For: 두 항목의 차이 자체가 메시지인 쪽(`pick-contrast` 를 쓴다), 항목이 여섯을 넘는 쪽(열형으로 바꾼다)
- Good With: `closing-band` · `well-tray` · `popup-card`
- Avoid With: `twin-window` (한 쪽에서 조작 대상이 둘로 갈린다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 항목 | (80, 284) 폭 860 또는 1160, 행 사이 8 | 최소 높이 72. 이름 32/700, 부연 24/500 또는 20/500 |
| 펼침 칸 | 열린 항목 아래, 같은 폭 | 항목 안에서 아래로 열린다 |
| 곁 칸 | 오른쪽 남는 폭 | 문장 폭 상한 700. 없어도 된다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 항목을 다 눌러 봤을 때 켜진다 |

- 하나만 열리는 무대인지 누른 것이 다 남는 무대인지를 원고가 정한다. 구현이 고르지 않는다.
- 고른 항목에만 흰 면과 링이 걸리고 나머지는 배경 위의 글줄로 남는다. 내용만 펼쳐지고 마는 조작을 두지 않는다.
- 항목이 열릴 때 아래 항목이 밀리는 것은 허용한다. 내용의 전개이지 피드백 이동이 아니다. 다만 다 열린 상태에서도 무대 아래 끝 824 를 넘지 않게 한다.
- 항목이 순서를 가지면 세 가지 무게로 구분한다. 지나온 항목과 지금 항목과 아직 오지 않은 항목이다. 잇는 선을 그리지 않는다.

### pick-contrast · 대비 무대

같은 규격의 두 블록이 같은 높이에 나란하고 조작이 둘의 차이를 표시한다.

- 조작 문법: `g-pick` 또는 `g-toggle`
- 논리 구조: `m-contrast`
- 등급: 2
- Best For: 두 답과 두 조건과 두 상태의 차이가 메시지인 쪽
- Avoid For: 셋 이상을 견주는 쪽. 셋을 나란히 두면 무엇이 주장인지 배치로 구분되지 않는다
- Good With: `well-tray` · `closing-band`
- Avoid With: `pick-list` (같은 쪽에서 고르기 문법이 두 층으로 갈린다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 좌 블록 | (80, 284) 대등 짝 x540 | 조작 가능하면 흰 면, 잠겨 있으면 우물 면. 폭은 1920 기준 860 이고 대등 짝 비율로 늘어난다 |
| 우 블록 | 좌 블록 오른쪽 gap 40 | 좌와 같은 규격 |
| 블록 이름 | 블록 상단 | 32/700. 무대의 부품이라 제목 계산에서 빠진다 |
| 블록 본문 | 이름 아래 32 | 대응 항목은 두 블록에서 같은 y 에 정렬한다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 양쪽을 다 눌러 봤을 때 켜진다 |

- 두 블록의 면을 다르게 하면 그 차이가 곧 상태 표시다. 잠긴 쪽은 면이 없고 열린 쪽만 흰 면을 갖는다.
- 두 블록을 한 껍데기에 담지 않는다. 흰 창 하나가 둘을 감싸면 그 창이 화면에서 가장 큰 무게를 갖고 두 블록은 그 안의 조각으로 읽힌다. 대신 우물 트레이가 둘을 하나의 견줌 장치로 묶는다.
- 맺음은 어느 블록 아래에도 붙이지 않는다. 결론의 근거가 된 블록이 무엇인지는 맺음이 켜질 때 그 블록에 함께 켜지는 교차 강조로 표시한다.
- 가운데 VS 원과 화살표 장식을 두지 않는다. 정렬로 대비를 만든다.
- 잠긴 블록도 눌린다. 눌러도 열리지 않고 왜 잠겨 있는지가 그 블록 안에 표시된다. 그래서 커서는 `not-allowed` 가 아니라 `pointer` 다. 실제로 눌리는 것에 `not-allowed` 를 주면 커서 표시와 동작이 어긋난다.

### toggle-reveal · 펼침 무대

접힌 상태와 펼친 상태를 오가며 그 높이 차이 자체가 메시지가 되는 판이다.

- 조작 문법: `g-toggle`
- 논리 구조: `m-decompose`
- 등급: 2
- Best For: 보이는 것과 실제로 담긴 것의 크기 차이가 메시지인 쪽
- Avoid For: 접힌 상태가 빈 상자인 쪽. 정지 상태에서 화면이 스스로 설명되지 않으면 이 판을 쓰지 않는다
- Good With: `well-tray` · `closing-band`
- Avoid With: `advance-machine` (전진과 뒤집기가 한 쪽에서 겹치면 어느 것이 진행인지 구분되지 않는다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 접힘 판 | 아래 끝 824 에 고정하고 위쪽으로 늘어난다 | 접힌 높이는 담긴 한 줄만큼 |
| 담기는 조각 | 판 안 우물 면, 조각 사이 16 | 파일 칩 규격 |
| 곁 열 | 반대 열 | 개념 문장과 소제목 블록 |
| 맺음 밴드 | (80, 아래 끝 1000) | 펼쳐 보고 곁 조작까지 마쳤을 때 켜진다 |

- 늘어나는 방향을 판마다 정하고 문서에 기록한다. 아래 끝을 고정하고 위쪽으로 늘어나면 아무것도 밀지 않고, 접힌 높이와 펼친 높이의 차이가 그대로 남는다.
- 고정 높이를 두면 그 차이가 사라지고 커다란 빈 상자만 남는다. 이 판에는 칸 예약 규칙을 적용하지 않는다.
- 높이에 전환을 걸지 않는다. 위 끝은 즉시 올라가고 새로 표시된 조각만 불투명도로 또렷해진다.
- 접기와 펼치기를 여러 번 오갈 수 있게 한다. 발표자가 되풀이해 보이는 것이 이 판의 값어치다.

### advance-machine · 기계 무대

장치 하나가 무대 가운데에 크게 있고 버튼 하나가 그것을 다음 단계로 옮긴다.

- 조작 문법: `g-advance` 또는 `g-toggle`
- 논리 구조: `m-process`
- 등급: 단계가 정해져 있으면 2, 확률이나 상태에 따라 결과가 달라지면 3
- Best For: 되풀이되는 절차 자체가 메시지인 쪽, 한 값만 바뀌고 나머지는 그대로임을 보이는 쪽
- Avoid For: 조작 전에 화면이 비어 있는 쪽. 버튼을 눌러야 무언가 표시되는 구조를 만들지 않는다
- Good With: `closing-band` · `popup-card` · `step-dots`
- Avoid With: `twin-window` (조작부의 소속이 장치와 창으로 갈린다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 장치 | (80, 284) | 곁 블록이 없으면 전폭이고 있으면 넓은 짝의 주열(1920 기준 1160)이다 |
| 장치 머리 행 | 장치 맨 위 | 지금 상태를 표시하는 칸. 미완성 문장이나 구간 막대 |
| 장치 본체 | 머리 행 아래 40 | 후보 목록, 막대, 조각. 정지 상태에서 이미 펼쳐져 있다 |
| 조작부 | 장치 안 맨 아래 행 | 왼쪽에 단계 점, 오른쪽에 버튼. 버튼 최대 2개 |
| 곁 블록 | 넓은 짝의 딸림 열, 세로 중앙 | 문장 폭 상한 700. 없어도 된다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 마지막 단계에 도달했을 때 켜진다 |

- 장치 전폭과 곁 블록은 같은 쪽에 두지 않는다. 곁 블록을 두는 순간 장치는 넓은 짝의 주열로 줄어든다. 이 조합 규칙은 `demos/subagents` 3쪽에서 실측으로 확정했다.

- 조작부는 장치 안에 넣는다. 버튼은 그 장치의 부품이다. 장치 밖 배경 위에 버튼을 따로 두면 무엇을 움직이는 버튼인지 형태로 구분되지 않는다.
- 장치는 무대 높이 540 을 다 쓴다. 장치가 짧아 아래가 남으면 항목 사이 간격을 늘리지 말고 후보 수를 늘리거나 쪽 구성을 다시 묻는다.
- 정지 상태에서 장치의 구성이 그대로 보인다. 미완성 문장과 후보와 확률이 처음부터 다 표시돼 있다.
- 전진 문법에는 단계 점을 둔다. 형태만 보고 지금 몇 번째인지 구분된다.
- 버튼 이름이 곧 설명이다. 두 줄짜리 사용법 안내를 화면에 두지 않는다.
- 마지막 단계에 도달하면 버튼 이름을 되돌림으로 바꾼다. 버튼을 감추거나 잠그지 않는다.

### advance-split · 쪼갬 무대

이미 놓여 있는 것을 조각으로 가르는 판이다. 없는 것을 불러오는 조작이 아니라 있는 것을 가르는 조작이다.

- 조작 문법: `g-advance`
- 논리 구조: `m-decompose`
- 등급: 2
- Best For: 한 덩어리가 실은 여러 조각으로 이루어져 있음을 보이는 쪽, 두 대상을 같은 방식으로 갈라 견주는 쪽
- Avoid For: 조각의 경계가 계산으로 정해지는데 그 값이 원고에 없는 쪽. 구현이 눈대중으로 나누면 사실이 틀린다
- Good With: `closing-band`
- Avoid With: `drag-threshold` (같은 쪽에서 나누기와 값 조절이 겹친다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 원문 행 | 장치 안 세로 중앙, 행 높이 56 | 32/700. 갈리기 전에는 문장 그대로 |
| 조각 행 | 같은 칸 | 조각 사이 8, 조각마다 radius 14 면 |
| 결과 수치 | 장치 아래 행 왼쪽 | 32/800 강조색. 둘 다 갈린 뒤에 켜진다 |
| 조작부 | 장치 아래 행 오른쪽 | 버튼 최대 2개 |
| 맺음 밴드 | (80, 아래 끝 1000) | 둘 다 갈린 뒤에 켜진다 |

- 두 대상이 처음부터 나란히 놓여 있다. 첫 버튼으로 하나를 가르고 둘째 버튼으로 나머지를 가른다.
- 견주기는 둘 다 갈린 뒤에 성립한다. 해설과 결과 수치를 조작 전에 표시하면 결론을 먼저 인쇄하는 것이 된다.
- 조각 경계와 개수는 원고가 실제 계산으로 확정한 값을 옮긴다. 계산으로 정해지는 값을 원고가 비워 두지 않는다.
- 조각 앞의 공백은 조각 안에 담는다. 조각이 낱말 단위가 아니라는 것이 거기서 드러난다.

### twin-window · 두 창 무대

왼쪽에서 조작하고 오른쪽에서 결과가 쌓이는 것을 본다.

- 조작 문법: `g-advance` 와 `g-point` 와 `g-input`
- 논리 구조: `m-accumulate`
- 등급: 상태가 수치로 누적되면 3, 항목만 쌓이면 2
- Best For: 조작한 것이 다른 곳에 쌓이는 과정이 메시지인 쪽
- Avoid For: 두 창의 내용이 서로 무관한 쪽. 그러면 두 판을 한 쪽에 담은 것이지 한 판이 아니다
- Good With: `point-map` · `step-dots` · `closing-band`
- Avoid With: `advance-machine` · `pick-contrast`

| 칸 | 위치 | 규격 |
|---|---|---|
| 조작 창 | (80, 240) 1160x584 | 흰 면, radius 32. 채팅 재현이거나 목록 |
| 관찰 창 | (1280, 240) 560x584 | 우물 면, radius 32, padding 24 |
| 조작부 | 조작 창 안 맨 아래 행 | 입력 바 안의 되돌림 버튼과 단계 점과 전송 버튼 |
| 맺음 밴드 | (80, 아래 끝 1000) | 마지막 조작이 끝났을 때 켜진다 |

- 860+860 변형은 두 창의 무게가 같을 때 쓴다.
- 이 판은 부제를 비우고 y 240 에서 시작하는 것이 기본이다.
- 조작 창은 정지 상태에서 비어 있지 않다. 이미 담긴 것과 다음에 보낼 것이 함께 표시된다.
- 한쪽에 마우스를 올리면 다른 쪽에서 대응하는 것에 호버 링이 켜진다. 배선 규격은 `point-map` 을 따른다.
- 말이 창을 넘으면 창 안에서 스크롤한다. 아래 끝을 따라가고 위로 더 있다는 것은 창 위 가장자리의 그러데이션이 표시한다.
- 조작 횟수가 셋을 넘으면 조작부에 단계 점을 둔다. 관찰 창의 게이지는 무엇이 쌓였는지를 표시하고 남은 조작 횟수는 표시하지 않는다.

### point-map · 짚기 무대

같은 것을 두 가지로 표현해 두고 한쪽을 가리키면 다른 쪽의 대응이 표시된다.

- 조작 문법: `g-point` 와 `g-pin`
- 논리 구조: `m-relation`
- 등급: 미리보기만 있으면 1, 고정까지 있으면 2
- Best For: 시각 표현과 언어 표현의 대응을 익혀야 하는 쪽, 범례와 본문의 연결
- Avoid For: 대응이 일대일이 아닌 쪽. 하나가 여럿에 대응하면 미리보기가 무엇을 켜야 할지 정해지지 않는다
- Good With: `twin-window` · `simulate-console` · `inspect-annotated`
- Avoid With: 없음. 이 판은 다른 판 안에 층으로 얹히는 것이 기본이다

| 칸 | 위치 | 규격 |
|---|---|---|
| 표현 A | (80, 284) 대등 짝 x540 | 우물 트레이. 항목마다 `data-k` 로 종류를 표시한다. 폭은 1920 기준 860 이고 대등 짝 비율로 늘어난다 |
| 표현 B | 표현 A 오른쪽 gap 40 | 같은 규격의 우물 트레이. 같은 `data-k` 로 짝을 맺는다 |
| 담긴 판 | 트레이 안 | 흰 면, radius 24. 높이는 (트레이 높이 - 안쪽 여백 16 - 판 사이 8x(N-1)) / N |
| 인스펙터 | 표현 A 가 놓인 판 안, 목록과 같은 칸 | 목록과 칸을 나눠 쓰고 불투명도로 교체한다. 없어도 된다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 대응을 모두 한 번씩 고정해 봤을 때 켜진다 |

- 두 표현을 같은 규격의 우물 트레이 둘에 담는다. 열 격자는 대등 짝이다. 같은 것을 두 가지로 표현한 판이므로 어느 쪽도 더 크지 않다.
- 담긴 판의 높이는 트레이 높이를 항목 수로 나눠 정한다. 트레이가 무대 높이를 정확히 채운다.
- 두 표현의 항목을 같은 y 에 정렬하지 않는다. 정렬만으로 대응이 드러나면 짚어 볼 이유가 사라진다.
- 호버는 미리보기이고 클릭은 고정이다. 고정된 항목에는 고른 것의 링이 걸리고 마우스를 치워도 표시가 남는다.
- 고정은 한 번에 하나다. 다른 항목을 누르면 그리로 옮겨 가고 같은 항목을 다시 누르면 풀린다.
- 고정 중에는 호버가 고정된 대응을 바꾸지 않는다. 발표자가 마우스를 옮기며 말해도 화면이 흔들리지 않아야 한다. 다만 마우스를 올린 항목 자체에는 호버 링이 걸린다. 커서가 pointer 인데 아무 반응이 없으면 커서가 거짓 신호를 보내는 상태라서 `hover-response` 가 반려한다.
- 링은 세 단이다. 정적 폴백의 옅은 링과 호버 링과 고른 것의 링이다. 세 단이 색으로 갈린다.
- 정적 폴백은 대응 전부에 옅은 링을 함께 거는 상태다. 그때 고정은 풀린다.
- 인스펙터는 목록 아래에 새 칸을 만들지 않고 목록과 같은 칸을 나눠 쓴다. 아래에 칸을 더하면 그 판의 높이가 조작에 따라 달라진다.
- 인스펙터는 대응 항목의 이름이 곧 설명인 쪽에는 두지 않는다. 그때 인스펙터를 두면 원고에 없는 글자를 구현이 만들게 된다.
- 조작 안내 문장을 화면에 적지 않는다. 커서와 호버 링과 고정 링이 문법을 표시한다.
- 이 규격의 출처는 `demos/subagents` 6쪽이다. 좌표와 담긴 판 높이와 인스펙터의 선택 여부와 링 세 단을 그 구현에서 실측해 이 절에 적었다.

### inspect-annotated · 주석 무대

본문 안에 주석 표시가 박혀 있고 그 주석이 달린 줄을 눌러 확인 결과를 받는다.

- 조작 문법: `g-pick` 와 `g-point`
- 논리 구조: `m-decompose`
- 등급: 2
- Best For: 형식을 갖춘 것을 하나씩 확인해 봐야 진위가 갈리는 쪽
- Avoid For: 확인할 것의 개수를 미리 알려도 되는 쪽. 개수를 알리면 청중이 세면서 읽어 검증 대상으로 바뀐다
- Good With: `point-map` · `closing-band`
- Avoid With: `pick-list` (같은 쪽에서 고르기 대상이 목록과 본문으로 갈린다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 본문 창 | 넓은 열 | 흰 창. 주석이 그 안에 박혀 있다 |
| 주석 표시 | 줄 끝 | 위첨자 글자. `sup` 태그를 쓰지 않는다 |
| 조작 영역 | 그 주석이 달린 줄 전체 | 보이는 표시는 작게 두고 누르는 곳만 넓힌다 |
| 주석 목록 | 본문 창 안 아래쪽 | 20/500. 창 밖으로 뽑지 않는다 |
| 확인 결과 | 주석 줄 오른쪽 칸 | 정지 상태에 칸이 잡혀 있고 글자만 표시된다 |

- 주석을 목록으로 뽑아내지 않는다. 뽑으면 검증할 대상으로 보이고, 본문 안에 박혀 있으면 그냥 읽고 넘어가게 된다.
- 위첨자는 무대에서 겨냥하기에 너무 작다. 조작 영역은 흐름 밖의 `::after` 로 넓히고 글자가 표시되는 위치는 그대로 둔다.
- 정지 상태에서 누를 수 있다는 것을 밑줄 면 띠로 표시한다. 팝업 낱말의 실선 밑줄과 형태를 구분한다.
- 호버는 그 주석이 대응하는 주석 줄을 미리 보이고 클릭은 확인 결과를 고정한다. 미리보기가 확인 결과를 앞당겨 표시하지 않게 한다.

### drag-compare · 겹침 비교

두 상태를 겹쳐 두고 경계를 끌어 옮기며 견준다.

- 조작 문법: `g-drag`
- 논리 구조: `m-contrast`
- 등급: 2
- Best For: 같은 화면의 전후 차이가 메시지인 쪽, 이미지나 재현 화면의 비교
- Avoid For: 두 상태의 구조가 달라 같은 좌표에 겹칠 수 없는 쪽. 그때는 `pick-contrast` 를 쓴다
- Good With: `closing-band`
- Avoid With: `pick-contrast` (같은 쪽에서 대비 문법이 둘로 갈린다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 겹침 판 | (80, 284) 1760x540 | 흰 면, radius 32, overflow hidden. 두 층이 같은 좌표에 놓인다 |
| 층 | 판 안 전면 | 아래층은 그대로 두고 위층만 `clip-path: inset(0 0 0 경계)` 로 오른쪽을 드러낸다 |
| 경계 핸들 | 판 안 세로 전체 | 폭 3 의 경계와 지름 56 의 원형 핸들 |
| 층 이름 | 각 층 안 위쪽, 자기 쪽 끝 | 20/700 알약 배지 |
| 항목 | 층 안, 두 층에서 같은 y | 두 층의 같은 항목이 같은 좌표를 쓴다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 핸들을 양 끝까지 옮겨 봤을 때 켜진다 |

- 핸들 지름은 56 이상이다. 무대에서 겨냥할 면적을 지킨다.
- 핸들은 키보드로도 옮길 수 있게 한다. 마우스가 없는 무대에서 이 판의 조작이 불가능해지면 안 된다.
- 정지 상태의 경계는 가운데가 아니라 한쪽 끝에서 조금 들어온 곳이다. 이유가 두 가지다.
  - 가운데에 두면 어느 쪽이 원래 상태인지 구분되지 않는다.
  - 핸들이 두 층의 항목 한가운데를 덮으면 그 항목의 중심점을 겨냥할 수 없다. 호버 전수 검사는 요소의 중심점에 마우스를 올리므로 그 항목이 통째로 검사에서 죽는다. `demos/prompt-caching` 3쪽은 62% 로 확정했다.
- 이 판은 `no-position-shift` 의 예외가 아니다. 경계는 `left` 로 옮기고 겹침 공개는 `clip-path` 나 폭으로 낸다. `transform` 을 쓰지 않는다.
- 두 층의 같은 항목에 교차 표시를 건다. 한 층의 항목을 짚으면 다른 층의 같은 항목에도 링이 걸려 링이 경계를 가로질러 이어진다. 겨냥점이 아래층에 떨어져도 위층의 표시가 함께 켜지므로 검사와 체험이 같은 결과를 낸다.
- 층 이름 배지는 판 위가 아니라 각 층 안에 둔다. 판 위에 두면 그 층이 다 가려진 뒤에도 이름이 남아 무엇을 가리키는지 없어진다.
- 두 층에서 값이 같은 항목을 적어도 하나 둔다. 경계가 지나가도 이음매가 생기지 않는 줄이 있어야 차이가 어디에만 있는지가 형태로 증명된다.
- 이 규격의 출처는 `demos/prompt-caching` 3쪽이다. 층 구현 방식과 정지 위치와 교차 표시와 배지 소속을 그 구현에서 실측해 이 절에 적었다.

### drag-threshold · 값 조절 무대

값 하나를 끌어 옮기면 결과 분포가 함께 바뀐다.

- 조작 문법: `g-drag`
- 논리 구조: `m-distribution`
- 등급: 결과가 한 값이면 2, 여러 값이 동시에 바뀌면 3
- Best For: 입력값과 결과의 관계가 메시지인 쪽, 한도와 예산의 감각을 주는 쪽
- Avoid For: 값의 의미를 청중이 아직 모르는 쪽. 개념을 설명하기 전에 값을 조작하게 하지 않는다
- Good With: `closing-band` · `point-map`
- Avoid With: `advance-split`

| 칸 | 위치 | 규격 |
|---|---|---|
| 장치 | (80, 284) | 곁 블록이 없으면 전폭이고 있으면 넓은 짝의 주열(1920 기준 1160)이다 |
| 장치 머리 행 | 장치 맨 위 | 현재 값과 단위 |
| 결과 표시 | 머리 행 아래 40 | 게이지 또는 막대 목록. 게이지 높이는 112 |
| 기준 표기 | 게이지 아래 16, 높이 30 | 20/500. 호버 상세와 같은 칸을 나눠 쓴다 |
| 조작부 | 장치 안 맨 아래 행 | 트랙 높이 14, 핸들 지름 56 |
| 곁 블록 | 넓은 짝의 딸림 열, 세로 중앙 | 문장 폭 상한 700. 없어도 된다 |
| 맺음 밴드 | (80, 아래 끝 1000) | 조작이 정한 조건에 닿았을 때 켜진다 |

- 장치 전폭과 곁 블록은 같은 쪽에 두지 않는다. 곁 블록을 두는 순간 장치는 넓은 짝의 주열로 줄어든다. `advance-machine` 과 같은 규칙이 이 판에도 걸린다.
- 결과 게이지의 높이는 구간 막대의 두 배인 112 다. 56 으로 두면 장치가 머리 행과 게이지와 조작부 셋뿐이라 무대 높이 540 을 못 채우고 빈 칸이 두 군데 남는다. `demos/prompt-caching` 5쪽에서 실측으로 확정했다.
- 값이 바뀌는 동안 결과 표시의 높이와 위치가 변하지 않게 한다. 폭과 색만 바뀐다. 색은 데이터 사다리의 단을 값에 따라 옮긴다.
- 값의 눈금을 트랙 위에 인쇄하지 않는다. 현재 값은 장치 머리 행이 표시한다.
- 게이지에 호버해 표시하는 계산 내역은 기준 표기와 같은 칸을 불투명도로 나눠 쓴다. 아래에 칸을 더하면 장치의 높이가 조작에 따라 달라진다.
- 맺음이 켜지는 조건은 그 쪽의 원고와 인터랙션 명세가 정한다. 양 끝을 다 지나는 것은 기본값이고, 값 자체가 메시지인 쪽에서는 그 값의 문턱이 조건이 된다.
- 핸들은 키보드 좌우 키로도 움직인다. 그때 쪽 넘김과 충돌하므로 핸들이 초점을 가진 동안에만 좌우 키를 가로채고 초점을 잃으면 즉시 넘김으로 돌려준다.
- 핸들과 트랙은 `drag-compare` 의 경계 핸들과 같은 부품이다. 한 발표에서 끌기가 두 쪽에 걸리면 같은 형태를 쓴다.

### input-sandbox · 입력 무대

발표자가 실제로 값을 넣고 그 결과를 받아 보이는 판이다.

- 조작 문법: `g-input`
- 논리 구조: `m-process`
- 등급: 3
- Best For: 청중이 직접 해 볼 것을 무대에서 먼저 한 번 보여야 하는 쪽
- Avoid For: 결과가 외부 호출에 달린 쪽. 무대에서 네트워크에 의존하는 조작을 두지 않는다
- Good With: `workshop-steps` · `closing-band`
- Avoid With: `simulate-console` (한 발표에서 입력 무대와 콘솔이 둘 다 있으면 조작 문법이 늘어난다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 입력 창 | 무대 왼쪽, 넓은 짝의 주열 | 흰 면. 입력 바가 창 맨 아래 행 |
| 결과 창 | 무대 오른쪽, 넓은 짝의 딸림 열 | 우물 면, padding 24 |
| 이력 | 입력 창 안, 입력 바 위 | 항목 높이 96, 항목 사이 8. 아래 끝을 고정하고 위쪽으로 쌓인다 |
| 입력 바 | 입력 창 맨 아래 | 높이 88. 필드 높이 56, 전송 원 지름 56 |
| 구간 막대 | 결과 창 안, 머리 아래 24 | 높이 56. 트랙 위에서 구간이 폭으로 갈린다 |
| 범례 | 막대 아래 24 | 행 높이 96. 색 점과 이름과 값 |
| 해설 칸 | 결과 창 안 아래쪽, 높이 58 | 20/500. 두 줄 칸을 미리 잡는다 |
| 폴백 | 결과 창 안 | 미리 준비한 결과를 즉시 표시하는 조작 |

- 창 좌표는 무대 좌표를 그대로 따른다. 부제가 있는 쪽은 (80, 284) 에서 시작하고 높이가 540 이며, 부제를 비운 쪽만 (80, 240) 에서 시작하고 높이가 584 다. 원고가 부제를 주면 부제를 지우지 않는다.
- 정지 상태에 첫 조작을 이미 마친 상태를 둔다. 두 창이 다 비어 있으면 어느 쪽이 조작 창이고 어느 쪽이 관찰 창인지 형태로 갈리지 않는다. 첫 결과가 결론이 아니라 기준선인 원고에서만 이렇게 한다.
- 결과 창에서 겨냥하는 것은 범례 행이고 막대 구간은 그 응답이다. 구간은 값에 따라 폭이 0 이 되므로 겨냥 대상이 될 수 없다.
- 막대 트랙 면과 구간 색을 데이터 사다리에서 두 단 이상 떼어 놓는다. 붙어 있으면 빈 트랙이 꽉 찬 구간으로 읽힌다.
- 되돌림은 전송 조작의 순환으로 낸다. 마지막 조작 뒤에 한 번 더 누르면 정지 상태로 돌아간다. 원고가 버튼 이름을 주지 않으면 글자 있는 버튼을 새로 만들지 않는다.
- 입력 칸에 깜빡이는 커서를 두지 않는다. 무대에서 깜빡임은 시선을 뺏고 캡처마다 다른 그림이 나온다. 필드 면과 전송 원의 형태가 입력 칸임을 알린다.
- 조작 횟수가 셋이면 단계 점을 두지 않는다. 이력에 쌓인 항목 수가 이미 진행 표시다.
- 입력 결과는 미리 준비한 값에서 고른다. 무대에서 실제 모델을 호출하지 않는다.
- 입력 필드가 초점을 가진 동안에는 좌우 키가 쪽을 넘기지 않는다. 초점을 잃으면 즉시 복구한다.
- 준비한 값 가운데 어느 것도 맞지 않으면 준비한 기본 결과를 낸다. 무대에서 빈 결과를 표시하지 않는다.
- 이 규격의 출처는 `demos/prompt-caching` 4쪽이다. 창 좌표와 정지 상태와 겨냥 대상과 되돌림 방식을 그 구현에서 실측해 이 절에 적었다.

### simulate-console · 시뮬레이션 콘솔

재생 가능한 모델과 탐색과 인스펙터를 한 화면에 둔 판이다. 이 스킬의 품질 기준 실물 표본이 이 판이다.

- 조작 문법: `g-advance` 와 `g-point` 와 `g-pin`
- 논리 구조: `m-process` 와 `m-distribution`
- 등급: 3
- Best For: 시스템의 동작 자체가 메시지인 쪽, 무엇이 얼마나 쌓이는지를 체험으로 증명해야 하는 쪽
- Avoid For: 등급 2 로 충분한 쪽. 이 판은 한 발표에 하나까지다
- Good With: `point-map` · `step-dots` · `closing-band`
- Avoid With: `advance-machine` · `input-sandbox`

| 칸 | 위치 | 규격 |
|---|---|---|
| 전역 상태 | 관찰 창 맨 위 | 누적 수치 32/800 과 한도 표기 20/500 과 누적 게이지. 원고가 그 글자를 줄 때만 둔다 |
| 종류 범례 | 상태 아래 16 | 행 높이 24. 종류마다 색 점과 이름과 값. 원고가 그 글자를 줄 때만 둔다 |
| 인스펙터 | 범례와 같은 칸 | 가리킨 종류의 설명. 범례와 불투명도로 교체한다 |
| 단계 해설 | 관찰 창 안 아래쪽, 높이 58 | 20/500. 두 줄 칸을 미리 잡는다 |
| 이벤트 창 | 조작 창 | 조작할 때마다 아래로 쌓이고 창 안에서 스크롤한다 |
| 조작부 | 조작 창 맨 아래 행 | 되돌림 버튼과 단계 점과 전진 버튼 |
| 맺음 밴드 | (80, 아래 끝 1000) | 마지막 단계까지 갔을 때 켜진다 |

- 세 층을 분리한다. 항상 보이는 전역 상태와 시간 순서의 이벤트와 요청 시 상세다. 화면이 한 번에 모든 것을 표시하지 않는다.
- 관찰 창의 칸 구성은 원고가 정한다. 표의 칸 가운데 원고에 글자가 없는 칸은 만들지 않는다. `demos/subagents` 4쪽은 전역 상태와 범례 없이 보고 목록과 인스펙터로 성립했다.
- 시작 전 상태를 따로 만든다. 아직 아무 일도 일어나지 않은 화면과 시작 버튼 하나가 있고, 그 상태에서도 화면이 스스로 설명된다.
- 되돌림 버튼을 둔다. 발표자가 임의 시점에 처음으로 돌아갈 수 있어야 한다.
- 수치는 구체적인 예시값으로 채우고 예시라는 것을 한 곳에 표기한다. 추상 도형으로 대신하지 않는다.
- 단계마다 교체되는 해설은 관찰 창 안에 둔다. 창 밖 절대 좌표에 띄우면 그만큼 창을 늘릴 수 없다.

### accumulate-recap · 누적 정리

항목이 하나씩 켜지고 다 켜지면 선언이 완성된다.

- 조작 문법: `g-advance`
- 논리 구조: `m-declare`
- 등급: 1~2
- Best For: 한 장을 닫는 마지막 쪽, 앞에서 확인한 것을 하나씩 되짚어 한 문장으로 묶는 쪽
- Avoid For: 새 내용을 처음 꺼내는 쪽. 되짚기 항목은 그 발표에서 이미 확인한 것만 담는다
- Good With: `chapter-cover`
- Avoid With: `closing-band` (선언이 맺음을 겸하므로 맺음 밴드를 따로 두지 않는다)

| 칸 | 위치 | 규격 |
|---|---|---|
| 머리 행 | (80, 80) 1760x24 | kicker 와 쪽 번호 |
| 선언 | (80, 140) 1760 | 88/800, 최대 2줄. 키워드 1~2개만 강조색 |
| 소제목 | (80, 396) | 32/700. 처음부터 검정이다. 없어도 된다 |
| 되짚기 항목 | (80, 452) 전폭, 2열 x ceil(N/2)행 | 열 폭은 대등 짝, 행 사이 20. 꺼진 것과 켜진 것을 색으로 구분한다 |
| 다음 물음 | (80, 아래 끝 1000) 폭 1160 | 24/500, 최대 2줄 |

- 소제목과 되짚기 항목은 같은 32/700 이고 색만 갈린다. 소제목은 처음부터 검정이고 항목은 꺼진 채로 시작한다. 조작이 끝나기 전까지 무엇이 머리인지 색으로 구분된다.
- 이 판에는 제목 64 가 없다. 선언 88 이 제목을 겸한다. 한 쪽에 64 이상은 하나라는 규칙을 지킨다.
- 항목은 누르는 대상이 아니라 발표자가 넘길 때마다 켜지는 대상이다. 흰 면을 주지 않는다.
- 선언의 뒷줄은 항목이 다 켜진 뒤에 불투명도로 켜진다. 그때까지 칸은 비어 있고 높이는 잡혀 있다.
- 항목에 쪽 번호를 인쇄하지 않는다. 원고에 없는 글자를 구현이 만들지 않는다.
- 항목 수 N 은 원고가 정하고 행 수는 ceil(N/2) 다. 소제목이 없으면 항목이 (80, 396) 을 이어받고 행 높이를 늘려 아래 끝을 다음 물음 위 40 까지 쓴다. `demos/subagents` 7쪽(항목 여섯, 행 높이 96)에서 실측으로 확정했다.

### workshop-steps · 실습 무대

두 창 무대의 변형이다. 조작 창에 단계 목록이 들어가고 관찰 창이 참가자 화면을 복제한다.

- 조작 문법: `g-pick`
- 논리 구조: `m-order`
- 등급: 2
- Best For: 참가자가 자기 기기에서 따라 하는 시간
- Avoid For: 단계가 넷을 넘는 실습. 넷을 넘으면 화면 하나로 복귀시킬 수 없다
- Good With: `input-sandbox` · `closing-band`
- Avoid With: `advance-machine`

| 칸 | 위치 | 규격 |
|---|---|---|
| 시간 배지 | 제목 행 우측 끝 1840 | 알약 배지 |
| 단계 | (80, 284) 폭 860, 단계 사이 8 | 최대 3개. 지시 32/700, 부연 24/500 또는 20/500 |
| 참가자 화면 | (1280, 284) 560x540 또는 860 | 참가자가 그 단계에서 보게 될 화면의 복제 |
| 맺음 밴드 | (80, 아래 끝 1000) | 마지막 단계에 도달했을 때 켜진다 |

- 단계를 진행에 따라 감추지 않고 전부 상시 노출한다. 따라오지 못한 사람이 이 화면만 보고 복귀한다.
- 참가자 화면은 묘사가 아니라 복제다. 자체 시스템의 일반 부품으로 교체하지 않는다.
- 단계 사이에 달라지는 것만 바뀐다. 같은 요청문은 같은 위치에 같은 글자로 남고 첨부 한 줄만 더해진다.
- 참가자 화면의 높이는 나중에 표시될 것의 칸까지만 잡는다. 아래를 넓게 비워 두지 않는다.

## 공통 부품

판을 가로질러 같은 규격으로 쓰는 부품이다. 판마다 다시 설계하지 않는다.

### closing-band · 맺음 밴드

체험 뒤에 해석을 문장으로 고정하는 칸이다. 인터랙션과 해석은 대체 관계가 아니라 역할 분담이다.

| 항목 | 규격 |
|---|---|
| 위치 | 왼쪽 x 80, 아래 끝 y 1000 |
| 면 | 우물 면, radius 24, padding 24 32 |
| 글자 | 32/700, 문장 폭 상한 1040 |
| 켜지는 조건 | 그 쪽의 조작을 다 마쳤을 때 |
| 전환 | 불투명도만 240ms. 위치 이동 없음 |

- 열다섯 쪽 어디서나 같은 위치라 발표자도 청중도 한 번만 익히면 된다.
- 무대 아래 끝 824 와 맺음 아래 끝 1000 사이 176 을 이 밴드가 확정으로 쓴다. 무대가 그 아래로 늘어나지 않는다.
- 결론이 그 발표 뒤로 미뤄지는 쪽에는 면 없는 예고 판을 대신 둔다. 면의 유무와 위치가 맺음과 예고를 구분한다.
- `accumulate-recap` 에는 두지 않는다. 선언이 맺음을 겸한다.

### control-bar · 조작부

버튼과 단계 점이 놓이는 행이다.

| 항목 | 규격 |
|---|---|
| 위치 | 장치 안 맨 아래 행 |
| 높이 | 56 |
| 버튼 | 높이 56, 최소 폭 160, 좌우 여백 28 |
| 단계 점 | 지름 10, 점 사이 12. 행 왼쪽 끝 |
| 되돌림 버튼 | 보조 색 면. 행 왼쪽 끝 또는 입력 바 왼쪽 |

- 버튼은 한 행에 최대 2개다. 셋을 넘으면 그 쪽의 조작이 너무 많은 것이다.
- 버튼 옆에 설명 글을 달지 않는다. 버튼 이름이 곧 설명이다.
- 조작이 셋을 넘는 시퀀스에는 단계 점을 둔다. 형태만 보고 몇 번째인지 구분된다.
- 본 조작이 하나뿐인 쪽에서만 강조색 버튼을 쓴다. 여럿이면 보조 색으로 내린다.

### well-tray · 우물 트레이

정지 상태에서 "이 중 하나를 고른다" 를 형태로 표시하는 담는 그릇이다.

| 항목 | 규격 |
|---|---|
| 면 | 우물 면, radius 32 |
| 안쪽 여백 | 8 |
| 담긴 판 사이 | 8 |
| 담긴 판 | 흰 면, radius 24 |

- 배경 위에 우물을 깔고 그 위에 흰 판을 올리면 밝기 층이 셋이 된다. 흰 판이 배경 위에 홀로 있을 때와 달리 고르는 것으로 읽힌다.
- 트레이 자체는 누르는 것이 아니므로 커서를 바꾸지 않는다. 담긴 판만 `pointer` 를 갖는다.
- 트레이는 무대의 높이를 정확히 채운다. 담긴 판의 높이는 트레이 높이에서 나눠 계산한다.

### popup-card · 팝업 카드

본문 안의 낱말 하나를 눌러 그 뜻을 여는 카드다.

| 항목 | 규격 |
|---|---|
| 폭 | 560 |
| 높이 상한 | 320 |
| 낱말과 카드 사이 | 12 |
| 면 | 흰 면, radius 24, padding 32 40 |
| 닫기 | 카드 오른쪽 아래, 보조 색 버튼 규격 |

- 모달이 아니다. 배경을 어둡게 깔지 않고 무대의 창과 장치 위로 올라가지 않는다.
- 무대를 덮는 범위를 판마다 정해 문서에 기록한다. 카드가 근거를 덮으면 그 조작이 무의미해진다.
- 카드의 기준이 되는 것은 낱말이 아니라 낱말이 담긴 블록이다. 낱말 하나만 피하면 그 문장의 나머지 줄이 카드 밑으로 들어간다.
- 카드 오른쪽이 x 1840 을 넘으면 오른쪽 끝을 1840 에 맞추고, 아래가 y 1000 을 넘으면 낱말 위로 띄운다.
- 닫는 방법은 셋이다. 닫기 버튼과 Esc 와 카드 밖 누르기다. 방향키는 카드를 닫으면서 쪽을 넘긴다.
- 다른 쪽에 갔다 오면 닫힌 상태로 돌아간다.

### step-dots · 단계 점

지금 몇 번째인지를 형태로 표시하는 부품이다.

| 항목 | 규격 |
|---|---|
| 지름 | 10 |
| 점 사이 | 12 |
| 꺼진 점 | 옅은 선 색 |
| 켜진 점 | 강조색 |
| 위치 | 조작부 행 왼쪽 끝 |

- 조작 횟수가 정해져 있고 셋을 넘을 때만 둔다. 둘 이하면 조작 대상 자체가 진행을 표시한다.
- 항목 목록이 조작 대상인 판에는 두지 않는다. 항목의 켜짐과 꺼짐이 이미 진행 표시다.
- 점을 누를 수 있게 만들지 않는다. 표시이지 조작부가 아니다.

## 복잡도 사다리 대응

`SKILL.md` 의 복잡도 사다리 0~3 등급과 판의 대응이다. 등급은 판이 정하는 것이 아니라 메시지가 정하고, 판은 그 등급을 담을 수 있는지로 걸러진다.

| 등급 | 형태 | 담을 수 있는 판 | 최소 요건 |
|---|---|---|---|
| 0 정적 | 다이어그램, 표 | `cover-stage` | 정지 상태로 구조가 다 보인다 |
| 1 공개 | 호버 공개, 클릭 팝업, 토글 | `chapter-cover` · `pick-list` · `point-map` · `accumulate-recap` | 상세를 감춰 인지 부하를 줄인다 |
| 2 단계 | 단계 진행, 상태 전환, 비교 | `pick-contrast` · `toggle-reveal` · `advance-machine` · `advance-split` · `twin-window` · `inspect-annotated` · `drag-compare` · `workshop-steps` | 조작 전후의 화면이 다르고 그 차이가 메시지다 |
| 3 시뮬레이션 | 재생 가능한 모델 + 탐색 + 인스펙터 | `simulate-console` · `input-sandbox` · `drag-threshold` · `twin-window` · `advance-machine` | 상태와 시퀀스와 상세가 분리돼 있고 되돌림이 있다 |

- 등급을 올리려면 근거 한 줄을 채널 배분 표에 적는다. 판을 바꾸는 것만으로 등급이 오르지 않는다.
- 등급 3 은 한 발표에 하나까지다. 둘 이상이면 청중이 익힐 조작이 너무 많다.
- 등급 2 이상은 조작 관찰 증거를 `qa/` 에 남겨야 완료로 친다.

## 조합 규칙

한 쪽 안의 조합과 한 발표 안의 조합을 함께 본다.

- 한 쪽에는 판 하나다. 두 판을 한 쪽에 담으면 그 쪽의 조작 문법이 둘로 갈리고 청중이 어디를 봐야 할지 정하지 못한다.
- 공통 부품은 판 위에 얹힌다. `closing-band` 와 `popup-card` 와 `well-tray` 와 `step-dots` 는 판을 바꾸지 않는다.
- `point-map` 은 예외로 다른 판 안에 층으로 얹힌다. 그 판의 조작 문법에 가리키기를 한 가지 더하는 것이므로, 원래 문법이 이미 둘이면 얹지 않는다.
- 같은 조작 문법이 이어지는 쪽을 붙여 배치한다. 조작 문법이 매 쪽 바뀌면 그 발표에서 청중이 익히는 것이 조작뿐이 된다.
- 한 발표에서 쓰는 판은 일곱 안팎으로 줄인다. ax-education 은 일곱 판으로 92쪽을 구현했다.
- 채팅 재현처럼 청중이 이미 아는 화면이 있으면 그것을 여러 판에서 되풀이해 쓴다. 되풀이가 학습 비용을 없앤다.

## 안정성 가드레일

판 선택과 무관하게 항상 적용한다. 여기 있는 것은 선택이 아니라 기본이다.

### overflow-containment · 넘침 제어

- 모든 쪽의 `scrollHeight` 가 1080 이하이고 `scrollWidth` 가 그때의 스테이지 폭(`offsetWidth`) 이하다.
- 조작을 다 마친 상태에서도 무대 아래 끝 824 를 넘지 않는다. 정지 상태만 재고 통과시키지 않는다.
- 좌우 여백 80 은 kicker 와 쪽 번호만이 아니라 보이는 요소 전수로 잰다. 그 둘만 재면 무대 안의 창이 스테이지 오른쪽 끝에 붙어 있어도 통과한다. `demos/subagents` 4쪽의 관찰 창이 그렇게 x 1360~1920 에 놓인 채 게이지를 통과했다.
- 자동 줄바꿈이 일어나지 않게 블록 폭을 최장 줄에 맞춘다. 줄 나눔은 원고가 정하고 화면 폭에 맡기지 않는다.
- `word-break: keep-all` 을 기본으로 걸고 flex 자식에 `min-width: 0` 을 준다.

### reserved-slot · 칸 예약

- 조작 뒤에 표시될 칸을 정지 상태에 미리 잡는다. 면도 글자도 없이 높이만 잡아 둔다.
- 미리 우물 면을 칠해 두지 않는다. 글자 없는 빈 상자가 화면에 남는다. 면은 글자와 함께 켜진다.
- 아래로 늘어나는 칸에만 적용한다. 아래 끝을 고정하고 위쪽으로 늘어나는 판은 이 규칙에서 빠진다.

### growth-direction · 늘어나는 방향

- 판마다 늘어나는 방향을 하나로 정하고 문서에 기록한다.
- 아래로 늘어나면 아래에 있는 것이 밀린다. 밀려도 되는 것은 그 판 안의 내용뿐이고 맺음 밴드와 머리 행은 밀리지 않는다.
- 위쪽으로 늘어나면 아무것도 밀지 않는다. 접힘과 펼침의 높이 차이가 메시지인 판은 반드시 이쪽이다.
- 높이에 전환을 걸지 않는다. 높이가 애니메이션되면 그 사이 프레임에서 아래 내용이 흔들린다.

### no-position-shift · 위치 이동 금지

- 등장과 상태 변화에 `transform` 을 쓰지 않는다. 불투명도와 흐림과 색과 폭만 바꾼다.
- `transform` 의 유일한 예외는 스테이지를 뷰포트에 맞추는 배율 하나다.
- 링은 `border` 가 아니라 `box-shadow` 로 넣는다. `border` 는 1px 밀림을 만들고 그 밀림이 이동으로 보인다.
- 링은 안쪽으로 넣는다. 바깥 링은 옆 항목의 칸을 침범한다.
- `prefers-reduced-motion: reduce` 환경에서는 전환 시간 토큰이 전부 0 이 된다. 움직임을 줄인 환경에서도 모든 상태에 도달할 수 있어야 한다.

### keyboard-recovery · 키보드 복구

- 조작 뒤에 방향키로 쪽을 넘기는 것이 즉시 된다. 조작 직후 `document.activeElement` 를 해제한다.
- 팝업이 열려 있어도 방향키는 팝업을 닫으면서 그대로 쪽을 넘긴다. 넘김을 막지 않는다.
- 입력 칸이나 슬라이더가 초점을 가진 동안에만 좌우 키를 가로챈다. 초점을 잃으면 즉시 넘김으로 돌려준다.
- 키 배치를 발표 전체에서 하나로 고정한다. 좌우는 쪽 넘김, Space 는 그 쪽의 다음 조작, End 는 그 쪽의 완료 상태다.
- 완료된 쪽에서 Space 는 그 쪽을 처음 상태로 되돌린다. 완료 후 동작이 쪽마다 다르면 발표자가 외울 수 없다. 완료 감지 표시는 맺음 요소의 `on` 이고 이 계약을 켠 덱은 `window.SPACE_RESTART` 를 참으로 둔다.
- 누르는 요소는 Tab 으로 초점을 받고 Enter 로 눌린다. 초점 표시는 `--ring-focus` 의 focus-visible 링이고 마우스 클릭에는 뜨지 않는다.

### hover-response · 호버 반응

- 커서가 pointer 인 요소는 전부 호버에서 보이는 반응을 낸다. 반응 없는 pointer 커서는 커서가 거짓 신호를 보내는 상태이고 반려 사유다.
- 반응의 형태는 세 가지 가운데 하나 이상이다: 안쪽 호버 링, 면 밝기 변화, 대응 요소의 교차 강조.
- 호버 반응의 전환 시간은 `--dur-base` 하나를 쓴다. 요소마다 다른 시간을 주지 않는다.
- `g-point` 요소의 호버는 반드시 대응 요소에도 표시를 켠다. 한쪽에만 걸린 교차 강조는 미완성이다.
- 호버로만 닿는 정보를 만들지 않는다. 호버 미리보기가 있는 곳에는 클릭 고정이 함께 있다.
- 검사는 자동 게이트로 한다. pointer 요소 전수에 mouseover 를 걸어 computed style 변화가 없는 것을 실패로 센다.

### static-fallback · 정적 폴백

- 모든 판에 완료 상태를 즉시 표시하는 함수를 둔다. 시간차 예약을 되풀이하는 방식으로 만들지 않는다.
- 완료 상태는 되풀이해 불러도 같은 화면이어야 한다. 예약이 남아 있으면 늦게 도착한 콜백이 화면을 뒤집는다.
- 완료 상태를 만들 때 창 안 스크롤은 부드러움을 끄고 단번에 아래 끝으로 보낸다. 그때의 부드러움은 곧 지연이다.

### stage-reset · 쪽 되돌림

- 다른 쪽에 갔다 오면 그 쪽의 조작은 처음 상태다. 발표는 앞뒤로 오가고, 돌아온 쪽이 눌린 채로 남아 있으면 다시 설명할 수 없다.
- 되돌림은 쪽을 떠난 뒤에 실행한다. 떠나는 전환이 보이는 동안 되돌리면 청중이 되돌아가는 화면을 본다.
- 예약된 타이머를 쪽마다 모아 두고 떠날 때 함께 제거한다. 남은 타이머가 다음 쪽에서 실행된다.

### stacking-discipline · 겹침 규율

- 겹치는 것은 팝업 카드와 시작 전 덮개 두 가지뿐이다. z-index 를 두 단계로 고정한다.
- 팝업이 무대의 무엇을 덮고 무엇을 덮지 않는지 좌표로 재서 문서에 기록한다.
- 시작 전 덮개는 말이 쌓이는 칸만 덮고 조작부는 덮지 않는다. 잠긴 조작부가 보여야 시작한 순간 잠김이 풀리는 것을 청중이 본다.

### self-contained · 자기완결

- 산출물은 외부 파일에 의존하지 않는 단일 HTML 이다. 웹폰트와 이미지를 base64 로 인라인한다.
- 무대에서 네트워크가 없어도 서체가 폴백으로 내려가지 않아야 한다. 서체가 바뀌면 줄 나눔 규격이 전부 무효가 된다.
- 서체는 실제 쓰는 글자로 서브셋해 넣는다. 서브셋 스크립트를 `qa/` 에 남겨 원고가 바뀌면 다시 생성한다.

## ax-education 인스턴스

ax-education 프로젝트의 일곱 판은 이 택소노미의 인스턴스다. `ax-education/디자인시스템.md` 의 레이아웃 판 절이 그 프로젝트의 규격이고, 이 문서는 그 규격을 일반화한 것이다.

| 프로젝트 판 | 택소노미 판 | 프로젝트 고유 결정 |
|---|---|---|
| 표지 판 | `cover-stage` | 워드마크 위치와 스크림 두 층 |
| 교시 표지 판 | `chapter-cover` | 여섯 질문 스트립 |
| 목록 무대 판 | `pick-list` | 하나만 열림과 다 남음을 원고가 쪽마다 정함 |
| 대비 무대 판 | `pick-contrast` | 면의 유무가 잠김과 열림 |
| 기계 무대 판 | `advance-machine` | 확률 뽑기와 구간 막대 |
| 두 창 무대 판 | `twin-window` | 채팅 재현을 조작 창으로 씀 |
| 누적 판 | `accumulate-recap` | 되짚기 항목 일곱 |
| 실습 판 | `workshop-steps` | 참가자 화면 복제 |

- 1교시 열네 쪽의 배정은 이렇다. 1쪽 `cover-stage`, 2쪽 `pick-list`, 3쪽 `pick-list`, 4쪽 `advance-machine`, 5쪽 `advance-machine`, 6쪽 `pick-contrast`, 7쪽 `twin-window`, 8쪽 `pick-contrast`, 9쪽 `workshop-steps`, 10쪽 `toggle-reveal`, 11쪽 `advance-split`, 12쪽 `simulate-console`, 13쪽 `inspect-annotated`, 14쪽 `accumulate-recap` 이다.
- 10쪽과 11쪽과 12쪽과 13쪽은 일곱 판 체계에서 각각 두 창 무대와 기계 무대와 두 창 무대와 목록 무대로 뭉뚱그려져 있었다. 택소노미가 조작 문법을 더 잘게 갈라 이름을 붙인 결과다.
- 프로젝트가 이 택소노미와 어긋나는 결정을 하면 프로젝트의 `디자인시스템.md` 가 우선한다. 이 문서는 그 결정을 다음 프로젝트에서 재사용하기 위한 기록이다.

## 판 고르는 절차

`SKILL.md` 의 3단계와 5단계에서 이 절차를 밟는다.

1. 그 쪽의 핵심 메시지 한 줄을 쓴다. 메시지가 없으면 판을 고르지 않고 구성으로 돌아간다.
2. 메시지의 논리 구조를 판정한다. 나열인가 대비인가 과정인가 누적인가 분해인가 관계인가 분포인가 선언인가.
3. 그 메시지가 정지 화면으로 증명되는지 변화를 보여야 하는지 체험시켜야 하는지를 묻고 복잡도 등급을 정한다.
4. 논리 구조와 등급으로 교차표에서 판 후보를 뽑는다.
5. 후보의 Best For 와 Avoid For 로 거른다. 거른 뒤에 남은 것이 없으면 가장 가까운 판의 변형으로 만들고 어느 판의 변형인지 명시한다.
6. 앞뒤 쪽의 판과 조합 규칙을 대조한다. 조작 문법이 매 쪽 바뀌면 붙여 배치할 수 있는지 다시 본다.
7. 정한 판 ID 를 `기획서.md` 의 채널 배분 절과 인터랙션 명세에 적는다. 이것이 있어야 다음 사람이 레이아웃을 임의로 바꾸지 못한다.
8. 안정성 가드레일 열 가지를 그 쪽의 구현 점검표에 그대로 넣는다.
