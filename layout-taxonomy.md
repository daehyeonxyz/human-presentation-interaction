# 레이아웃 택소노미 바인딩

## 원본

레이아웃 판단의 원본은 `~/.claude/skills/layout-taxonomy/taxonomy.md` 다. 6부 23분류 139개 키워드이고 사용자가 제공한 분류 체계다.

- 이 레포는 원본을 수정하지 않고 참조만 한다. 원본에 없는 구조가 필요하면 어느 패턴의 변형인지 명시한다.
- 레이아웃 결정은 원본의 패턴 ID 로 지목한다. `bento-grid` · `modular-grid` · `hierarchical-grid` · `swiss-grid` 처럼 원본 표의 ID 를 그대로 쓴다.
- 조합 규칙도 원본을 따른다. 각 패턴의 Good With 는 함께 쓰고 Avoid With 는 같은 화면에 두지 않는다.
- 안정성 가드레일 넷(`overflow-containment` · `intrinsic-sizing` · `cls-prevention` · `stacking-discipline`)은 원본이 정한 대로 항상 적용한다.

## 계보

이 문서는 프레젠테이션 스테이지 어댑터다. 원본이 다루는 것은 스크롤되는 웹 화면이고 이 레포가 다루는 것은 넘김으로 이어지는 한 쪽이라, 원본의 패턴을 무대에 앉힐 때의 규칙만 여기서 정한다.

- 조작 문법과 복잡도 등급 대응과 실물로 검증된 판 기록은 [조작문법.md](조작문법.md) 가 갖는다.
- 절대 좌표 규격은 두지 않는다. 프레임 안의 배치는 grid 의 fr 행과 stretch 가 정하고 쪽마다 y 값을 손으로 맞추지 않는다.

## 스테이지 계약

### 배율과 설계 크기

- 배율은 `s = min(vw / 1920, vh / 1080)` 이다.
- 설계 폭은 `clamp(1920, vw / s, 2560)` 이고 설계 높이는 1080 고정이다.
- 화면비가 맞지 않으면 레터박스가 남는다. 레터박스를 없애려고 설계를 비틀지 않고 레터박스 색은 그 쪽의 캔버스 색으로 둔다.

세로를 고정한 이유는 벤토 채움 때문이다. 발표 화면의 표준 화면비는 16:9 이고 세로를 1080 으로 못박으면 판의 세로 구성을 한 번만 설계한다. 세로까지 가변으로 두면 같은 판이 16:9 와 4:3 에서 다른 밀도를 요구해 쪽마다 구성을 두 벌 설계하게 되고, 그 두 벌 가운데 한 벌은 반드시 성기게 된다. 가로만 늘리면 열 수와 열 폭만 늘어나므로 한 벌로 두 화면비를 감당한다.

### 프레임

- 본문 쪽마다 레이아웃 루트를 하나 두고 `data-frame` 속성을 붙인다.
- 프레임의 상자는 여백 상자와 정확히 같다. `left: 80px; top: 80px; width: calc(100% - 160px); height: calc(100% - 160px)` 이다.
- 프레임은 12열 격자이고 칸 사이 간격은 24 하나뿐이다. 여백 80 과 격자 간격 24 밖의 값을 쓰지 않는다.
- 행 구성은 머리(24) · 표제(auto) · 본체(1fr) 이고, 본체 아래에 한 행을 더 두는 쪽만 네 번째 행을 붙인다.
- 본체 행이 남는 세로를 전부 먹는다. 아래 끝을 좌표로 맞추지 않고 `1fr` 과 `stretch` 가 보장한다.

### 채움

- 여백 안쪽을 벤토 그리드가 빈 데 없이 채운다. 셀 높이와 폭은 위계에 따라 다르다(`hierarchical-grid`).
- 항목이 적어 칸이 남으면 간격을 늘리지 않고 그 칸에 들어갈 정보를 늘린다. 원고가 모자라면 셀 단위로 필요한 글자를 목록으로 올리고 원고를 증보한다.
- 세로 밀도의 판정은 수치가 아니라 렌더 비교다. 상용 실물과 같은 배율로 나란히 놓고 성긴 쪽이 없는지 눈으로 본다. 캡처를 근거로 남긴다.
- 여백 80 침범은 통과 기준이고 쪽별 아래 끝 y 값은 참고 자료다.

### 면

- 면은 전부가 아니라 필요한 셀에만 준다. 조작 대상이거나 다른 층으로 갈라야 하는 셀만 면을 갖고 나머지는 배경 위에 글자를 직접 놓는다.
- 면 세 층은 흰 면과 캔버스와 우물이다. 흰 면은 만지는 것, 캔버스는 읽는 것, 우물은 보는 곳이다.
- 면 겹침은 두 겹까지다. 카드 안에 카드를 넣고 그 안에 또 카드를 넣지 않는다.
- 경계는 선이 아니라 얕은 명도 차로 만든다.

## 무대에서 쓰는 패턴

원본의 패턴 가운데 한 쪽 무대에 그대로 성립하는 것들이다. 무대에 앉힐 때의 번안만 적고 패턴의 정의는 원본이 갖는다.

| 원본 패턴 ID | 무대에서의 쓰임 |
|---|---|
| `bento-grid` | 프레임 전체의 기본 구성. 크기가 다른 셀로 한 쪽을 구획한다 |
| `hierarchical-grid` | 셀 크기를 위계에 맞춰 차등한다. 주 항목이 세로를 다 쓰고 나머지가 나눠 갖는 배치가 여기다 |
| `modular-grid` | 같은 규격 항목 셋 이상을 나열하는 정지 쪽 |
| `swiss-grid` | 표제와 본체를 격자선에 칼같이 붙이고 비대칭으로 세우는 구성 |
| `asymmetric-two-up` | 조작 창과 관찰 창을 다른 폭으로 나누는 좌우 구성 |
| `split-screen` | 같은 규격 둘을 대등하게 맞세우는 대비 구성 |
| `oversized-type` | 표지와 정리 쪽. 글자 크기와 여백만으로 화면을 장악한다 |
| `stat-band` | 관찰 창의 상시 표시 층. 수치 몇 개를 큰 활자로 나란히 세운다 |
| `progressive-disclosure` | 조작으로 상세를 여는 쪽. 등급 1 의 근거 패턴이다 |
| `focal-point` | 한 쪽에 지배적 진입점 하나. 64 이상 글자를 한 쪽에 하나만 두는 규칙의 근거다 |
| `scale-contrast` | 표제 밴드와 내용 밴드를 두 배 넘게 벌린다 |
| `spatial-grouping` | 묶음 안은 좁게 묶음 사이는 넓게. 격자 간격 하나로 처리한다 |
| `overflow-containment` | 가변 콘텐츠가 칸 밖으로 넘치지 않게 한다. 선택이 아니라 기본이다 |
| `stacking-discipline` | 겹치는 것은 팝업과 덮개 둘뿐이고 z-index 를 두 단계로 고정한다 |
| `cls-prevention` | 조작 뒤에 나타날 칸을 정지 상태에 미리 잡아 둔다 |

원본에서 무대에 쓰지 않는 것도 적어 둔다. 스크롤 구동 패턴 전부(`scrollytelling` · `parallax` · `scroll-snap-sections` 등)와 반응형 변형(`mobile-first-stack` · `reflow-reorder`)은 한 쪽이 한 화면에서 끝나는 이 무대에 성립하지 않는다.

## 판 고르는 절차

1. 그 쪽의 핵심 메시지 한 줄을 쓴다. 메시지가 없으면 구성으로 돌아간다.
2. 메시지의 논리 구조를 [조작문법.md](조작문법.md) 의 `m-*` 에서 판정한다.
3. 그 메시지가 정지 화면으로 증명되는지 변화를 보여야 하는지 체험시켜야 하는지를 묻고 복잡도 등급을 정한다.
4. 등급에 맞는 조작 문법을 `g-*` 에서 고른다. 한 쪽에 두 가지까지다.
5. 셀 지도를 먼저 그린다. 프레임의 어느 구역을 어떤 정보가 채우고 어느 셀이 면을 갖는지 적는다.
6. 그 셀 지도를 원본 택소노미의 패턴 ID 로 지목한다. 등급 0 정지 쪽은 `modular-grid` 나 `hierarchical-grid` 의 정지 구성으로 잡는다.
7. 셀 지도에서 원고가 비는 셀을 목록으로 뽑아 원고를 증보한다. 구현이 글자를 발명하지 않는다.
8. 정한 패턴 ID 와 셀 지도를 `기획서.md` 에 적는다. 이것이 있어야 다음 사람이 레이아웃을 임의로 바꾸지 못한다.
