---
name: interactive-presentation
description: 발표의 목적과 메시지를 먼저 잡고, 청중이 직접 보고 조작하며 이해하게 되는 발표 자료를 기획부터 구현과 검증까지 만들어 줍니다. "인터랙티브 발표 만들자", "체험형 프레젠테이션", "조작하는 슬라이드", "발표에 인터랙션 넣자" 같은 요청에 사용합니다. 화면에 인쇄될 글자의 문체 기준은 outputs 스킬에 의존합니다.
---

# interactive-presentation (인터랙티브 프레젠테이션 제작)

개념을 도형 몇 개로 그려 넣는 대신, 그 개념에 어떤 인터랙션이 필요한지 먼저 판정하고 실제 웹에서 구동되는 구현체 수준까지 만들어내는 스킬이다. 품질 기준의 실물 표본은 Claude Code 공식 문서의 "Explore the context window" 위젯이며, 그 해부와 지향점은 `research/north-star.md`에 있다. 발표 철학의 실측 근거는 `research/presentation-philosophy.md`, 인터랙션 함정과 방법론 근거는 `research/presentation-landscape.md`다. 세 문서는 필요할 때 Read하는 참조 소스이고, 이 스킬 본문이 실행 절차의 SSOT다. 트리거 어휘: "인터랙티브 발표", "체험형 프레젠테이션", "인터랙티브 슬라이드", "발표에 인터랙션 넣자".

레이아웃 판단의 원본은 `references/taxonomy.md`(사용자 제공 분류 체계, 6부 23분류 139개 키워드. 이 스킬에 번들돼 있다)다. `layout-taxonomy.md`는 그 원본의 프레젠테이션 스테이지 어댑터이고 스테이지 계약(배율·프레임·채움·면)을 갖는다. 조작 문법 `g-*` 와 논리 구조 `m-*` 와 복잡도 등급 대응과 안정성 가드레일은 `interaction-grammar.md`가 갖는다. 3단계 구성과 5단계 채널 배분과 7단계 디자인 시스템과 8단계 구현이 원본의 패턴 ID로 레이아웃을 지목한다.

레이아웃과 인터랙션과 무대에 관한 판단 재료는 이 폴더 안에 다 들어 있다. 폴더 밖에 의존하는 것은 `outputs` 스킬 하나이고 그것이 화면에 인쇄될 글자의 기준이다.

```text
SKILL.md               실행 절차의 SSOT (이 문서)
layout-taxonomy.md     원본 택소노미의 프레젠테이션 스테이지 어댑터 (스테이지 계약)
interaction-grammar.md 조작 문법 g-*, 논리 구조 m-*, 복잡도 등급 대응, 안정성 가드레일, 검증된 판 기록
references/taxonomy.md 레이아웃 판단의 원본 분류 체계
research/              지향점(north-star)과 발표 철학과 인터랙션 방법론 근거
ax-education/          실전 발표 프로젝트. 디자인 시스템과 원고와 산출물의 실물 표본
demos/                 검증 덱과 그 기획서·원고·인터랙션 명세
templates/stage.html   무대 계약 뼈대. 8단계 구현의 출발점
templates/plan.md      기획서 뼈대
templates/manuscript.md 슬라이드 원고 뼈대
checklists/            구현 자가 점검표(build-selfcheck)와 관찰 요청 대본(observation-request)
tools/fluid-gate.mjs   해상도 매트릭스 게이트 (실행 가능한 환경에서만)
```

## 의존성

**화면에 인쇄될 글자의 기준은 `outputs` 스킬이 갖는다.** 이 스킬은 무엇을 어떤 구조로 보여줄지를 정하고, 그 글자를 어떤 한국어로 쓸지는 `outputs` 가 정한다. 두 스킬의 관할이 겹치지 않게 아래로 고정한다.

| 정하는 것 | 관할 |
|---|---|
| 문체, 화계, 종결 방식, 리듬 수치, 금지 표현, 상투어 판정 | `outputs` |
| 제목의 형태(명사구), 슬라이드 본문의 종결, 대본의 구어체 | `outputs` 의 `standards/slides.md` |
| 줄바꿈 위치, 칸별 분량 상한, 셀 지도, 화면 위계 | 이 스킬 (화면의 제약이므로) |
| 무엇을 말하고 무엇을 조작시킬지, 페이지 메시지, 판과 등급 | 이 스킬 |

6단계 서술과 9단계 검증에서 `outputs` 의 다음 문서를 실제로 연다. 내용을 이 스킬로 복사하지 않는다 (bind-not-copy).

- `standards/judgment.md` · `standards/korean.md` · `standards/style.md` — 모든 산출물에 적용하는 공용 세 층
- `standards/slides.md` — 슬라이드 텍스트와 대본의 세부 기준
- `standards/genres/presentation.md` — 발표 자료의 말투 분포와 리듬 수치
- `standards/genres.md` 의 판정표 — 발표에서 정상인 순서 표현("먼저", "둘째", "흥미로운 점은")을 지우지 않기 위해 반드시 대조한다
- `standards/banned-words.json` — 금지 표현 대조
- `guides/review.md` — 9단계 검수 절차

`outputs` 가 없는 자리에서는 원고 단계를 그냥 진행하지 않는다. 먼저 사용자에게 요청한다. 그래도 없으면 문체 기준을 지어내지 말고 `research/presentation-philosophy.md` 의 실측 관행만으로 쓴 뒤, 원고 첫머리에 "문체 기준 미적용" 을 적고 사용자 확인을 받는다.

## 실행 환경

절차와 통과 기준은 환경과 무관하게 같다. 산출물이 놓이는 자리만 다르다.

- **파일을 쓸 수 있는 환경**: 아래 산출물 배치대로 폴더에 남긴다.
- **대화 환경**: 산출물을 파일 대신 대화 안의 문서로 유지한다. 단계마다 그 산출물을 한 번에 다 적어 사용자가 통과 기준을 눈으로 확인할 수 있게 한다. 최종 HTML 은 아티팩트로 낸다. **단계를 건너뛰는 것이 아니라 형식만 바뀐다.** 기획서 없이 원고로 가거나 원고 없이 구현으로 가면 대화 환경에서도 똑같이 전면 재작업이 된다.
- 두 환경 모두 **에이전트를 쓰지 않는 단독 실행이 기본**이다. 역할 분리는 사람이나 에이전트가 아니라 **순서와 통과 기준**이 만든다.

## 핵심 원칙 (계약)

1. **인터랙션이 곧 논증이다.** 페이지의 핵심 메시지는 텍스트로 주장되기 전에 조작과 변화로 체험된다. 메시지 전달에 기여하지 않는 장식용 인터랙션은 넣지 않고, 체험 뒤에는 해석 문장(takeaway)으로 못박는다.
2. **콘텐츠 설계가 인터랙션 설계다.** 어떤 단계로 끊고 어떤 항목에 어떤 설명을 붙일지는 전부 내용 결정이다. 인터랙션 뼈대를 먼저 만들고 내용을 붓지 않는다. 내용의 논리 구조가 인터랙션 구조를 결정한다.
3. **복잡도는 메시지가 요구할 때만 올린다.** 페이지마다 복잡도 사다리의 판정 질문을 통과한 등급만 쓴다. 등급 상향에는 근거 한 줄이 붙는다.
4. **페이지당 핵심 메시지 하나.** 메시지 한 줄을 먼저 쓰고 레이아웃은 그 메시지의 논리 구조를 시각화하는 수단으로 고른다. 메시지 없는 페이지는 만들지 않는다.
5. **원고가 먼저고 구현이 뒤따른다.** 화면에 인쇄될 글자는 원고 단계에서 확정한다. 구현은 그 글자를 화면에 앉히기만 하고 새 이름이나 새 문장을 발명하지 않는다. 이 순서를 어기면 화면에서 문장이 발명되고 전면 재작업이 된다. 혼자 실행하더라도 두 단계를 한 호흡에 섞지 않는다.
6. **층을 가른다.** 화면에 인쇄되는 글자와 기획·노트의 메모 층은 다른 문서다. 인쇄 층의 문체와 화계는 `outputs` 가 정한다. 슬라이드 텍스트는 명사로 끊고 청중에게 직접 말하는 층(도입, 전환, 대본)은 경어체라는 것이 그 기준의 골자이고, 세부는 `standards/slides.md` 와 `standards/genres/presentation.md` 를 연다. 이 스킬이 화계를 따로 정하지 않는다.
7. **줄바꿈은 쓰는 사람이 정한다.** 화면 폭에 맡기지 않는다. 원고의 줄 나눔이 화면의 줄 나눔이고 한 줄이 한 호흡이다. 넘치면 줄이는 것이 아니라 들어갈 길이를 알고 쓴다. 길이를 알 수 있도록 7단계가 칸마다 글자 수 상한을 정한다.
8. **근거 없는 주장 금지.** 수치, 인용, 사실 주장은 수집 시점부터 출처를 함께 붙인다. 출처 없는 재료는 [미확정] 표시로만 남고 슬라이드에 오르지 못한다. 시연과 실습의 소재는 가상으로 만들고 화면에 예시임을 표기한다.
9. **발표자 통제권과 폴백.** 모든 인터랙션은 무대에서 한 손으로 조작 가능하고(클릭 타깃 크게), 조작 후 키보드 내비게이션이 즉시 복구되며, 실패 시 보여줄 정적 폴백 상태를 갖는다. 인터랙션이 발표를 끌고 가지 않는다.
10. **완료 판정은 렌더 관찰과 조작 관찰이다.** 실제로 구동한 화면과 조작 확인만 완료 증거다. 코드 검토나 정적 검사만으로 완료를 선언하지 않는다. 브라우저를 띄울 수 없는 자리에서는 관찰하는 눈이 사용자다. `checklists/observation-request.md` 로 무엇을 눌러 무엇을 봐야 하는지 적어 주고 그 답을 증거로 삼는다. 관찰 없이 "완성했습니다" 라고 적지 않는다.

## 인터랙션 복잡도 사다리 (판정 기준표)

페이지마다 "이 페이지의 핵심 메시지는 정지 화면으로 증명되는가, 변화를 보여야 하는가, 체험시켜야 하는가"를 묻고 등급을 정한다. 근거는 `research/north-star.md`다.

| 등급 | 형태 | 언제 |
|---|---|---|
| 0 정적 | 잘 설계된 다이어그램, 표 | 구조가 정지 상태로 다 보일 때 |
| 1 공개 | 호버 공개, 클릭 팝업, 토글 | 상세를 숨겨 인지 부하를 줄일 때 (detail-on-demand) |
| 2 단계 | fragment 진행, 상태 전환, 비교 슬라이더 | 변화, 대비, 과정이 메시지일 때 |
| 3 시뮬레이션 | 재생 가능한 모델 + 탐색 + 인스펙터 | 시스템의 동작 자체가 메시지일 때 |

## 앵커링 경계

- 정적 슬라이드 생성기류의 절차, 미감, 템플릿을 기본값으로 가져오지 않는다. 그 관행을 끌어오면 지향점이 하향 앵커링된다. 검증된 개별 역학(고정 세로 기준 스테이지 스케일링 CSS, file:// 렌더 검증 규율 등)만 의식적으로 골라 쓰고, 절차와 스타일은 이 스킬이 스스로 정의한다.
- 인터랙션 asset 카탈로그를 선제 제작하지 않는다. 카탈로그는 실전에서 만든 구현체의 회고와 승격으로만 자란다.

## 산출물 배치

발표 프로젝트 폴더는 파이프라인 단계가 아니라 사용자의 사용 흐름을 기준으로 나눈다. 사고와 결정은 `기획서.md` 한 문서에 절로 쌓이고, 폴더는 재료와 결과물과 증거의 최소 구분만 둔다. 각 항목은 "사용자가 왜 여길 여는가"로 설명된다.

```text
기획서.md        발표의 사고와 결정 전부 (기획, 구조, 구성, 왕복 기록, 채널 배분). 왜 이렇게 만들었는지 보러 연다
슬라이드원고.md   화면에 인쇄될 모든 글자. 화면에 뭐라고 쓰였는지 보러 연다
발표노트.md       쪽별 요지와 개념 설명. 발표 준비하러 연다
디자인.md         사용자가 준 시각 기준. 무엇을 지켜야 하는지 보러 연다
디자인시스템.md    구현 규격 (색·타이포·격자·컴포넌트·레이아웃 판)
tokens.css        값의 단일 원본. 각 파일이 이 내용을 복사해 쓴다
인터랙션.md       조작이 어떻게 동작하는지 보러 연다
reference/       원천·청중·브랜드·기존 자료 조사. 근거를 확인하러 연다
assets/          로고, 웹폰트, 배경 이미지
deliverables/    선승인 목업과 최종 발표 HTML. 발표를 띄우러 연다
qa/              렌더 캡처와 조작 관찰 증거. 완료 판정 근거를 보러 연다
```

발표가 길어 파일을 나누면 `deliverables/`에 단위별로 하나씩 둔다. 각 파일은 독립 실행된다.

각 단계는 이 배치 안에 자기 산출물을 남기고 통과 기준을 만족한 뒤에 다음 단계로 간다.

대화 환경에서는 같은 산출물을 같은 이름의 문서로 대화 안에 유지한다. `기획서`는 절이 쌓이는 한 문서이므로 매번 처음부터 다시 적지 않고 그 단계에서 더해진 절만 적는다. `reference` 와 `assets` 는 사용자가 올린 자료 그 자체이고, `qa` 는 관찰 요청과 그 답변이 대신한다.

## 발표 규모에 따른 축약

전 단계가 모든 발표에 같은 무게로 필요하지는 않다. 다만 **줄일 수 있는 것은 각 단계의 분량이지 단계 자체가 아니다.** 어떤 규모에서도 못 건너뛰는 것이 셋 있다. 페이지마다 핵심 메시지 한 줄(3단계), 최소 한 번의 왕복(4단계), 구현 전 원고 확정(6단계). 이 셋을 건너뛴 발표는 예외 없이 전면 재작업이 됐다.

| 규모 | 축약 |
|---|---|
| 5~8쪽, 15분 이내 | 2단계 발산을 한 차례로 끝낸다. 2.5단계는 사용자가 준 자료만 본다. 7단계 목업은 1장으로 줄인다 |
| 9~20쪽, 30~60분 | 전 단계를 그대로 밟는다 |
| 20쪽 초과 | 단위별로 파일을 나누고 단위마다 6~9단계를 돈다. 1~5단계는 전체를 한 번에 잡는다 |

축약할 때도 그 단계를 어떻게 줄였는지 한 줄로 적는다. 조용히 건너뛴 것과 의식적으로 줄인 것은 다르다.

## 절차

단독으로 전 단계를 수행한다. 대신 **단계마다 자리를 바꿔 앉는다.** 원고는 원고 쓰는 사람의 입장에서 다 쓴 뒤 확정하고, 그다음에 규격을 짜고, 구현은 확정된 원고의 글자만 화면에 앉힌다. 6단계와 7단계와 8단계를 한 호흡에 섞으면 화면에서 문장이 발명되고 전면 재작업이 된다.

각 단계는 자기 산출물을 내고 통과 기준을 만족한 뒤에 다음으로 간다. **통과 기준을 스스로 판정한 결과를 매번 한 줄로 적는다.** 판정을 적지 않고 넘어가면 그 단계는 수행되지 않은 것이다.

사용자에게 확인을 받아야 넘어가는 지점은 셋이다. 1단계 기획, 4단계 왕복, 7단계 목업 선승인. 이 셋은 스스로 통과시킬 수 없다.

### 1. 기획

발표의 목적, 청중, 전체 핵심 메시지, 발표 상황(시간, 말로 커버할 범위)을 사용자 인터뷰로 확정한다. 결론 한 줄과 청중 체험 동선(이 발표를 들은 사람이 무엇을 가지고 나가는가)까지 잡는다.

- 산출물: `기획서.md`의 기획 절
- 통과 기준: 결론 한 줄, 발표 시간, 청중 체험 동선이 문서에 확정돼 있고 사용자가 확인했다.

### 2. 구조

목차 그 이상의 내용 구조를 두 국면으로 짠다.

- **발산**: 메시지를 전달하기 위해 담아야 할 내용 후보를 브레인스토밍과 자료 수집으로 포화시킨다. 새 것이 안 나올 때까지 파고, 닫기 전에 "빠진 것이 없는가"를 되묻는다. 수치, 인용, 사실 주장은 출처를 함께 수집한다.
- **배치**: 수집된 재료를 메시지 전달의 논리로 배열한다. 각 재료의 역할(훅, 정의, 대비, 근거, 체험, 성찰)을 정하고 무엇을 버릴지 결정한다. 구조를 먼저 고정하고 표면은 나중에 간다.

- 산출물: `기획서.md`의 구조 절 (재료 목록과 역할 배치). 수집한 원자료와 출처는 `reference/`에 둔다.
- 통과 기준: 남은 재료 전부에 역할이 있고, 수치와 인용에 출처가 붙어 있거나 [미확정]으로 표시돼 있다. 근거 없는 재료는 이 단계를 넘어가지 못한다.

### 2.5 실물 조사 (생략하면 뒤가 전부 반려된다)

규칙 문서만 읽고 시작하지 않는다. 실물을 눈으로 본다.

- **사용자의 지난 발표 자료를 렌더 이미지로 직접 본다.** 여러 편을 보고 구조, 강조 방식, 줄바꿈, 위계의 관행을 뽑아 `reference/`에 남긴다. HTML로 남아 있으면 렌더 캡처를, PDF로만 남아 있으면 이미지로 변환해서 본다.
- **발표가 특정 조직을 향하면 그 조직의 브랜드 자산과 실물 화면도 본다.** 공식 템플릿 파일이 있으면 열어서 실제 쓰인 서체와 색을 세어 확인한다. 추정하지 않는다.
- 로고와 서체 같은 브랜드 자산은 사용자에게 받는다. 서드파티 브랜드 자산을 직접 그리지 않는다.

- 산출물: `reference/`의 조사 문서, `assets/`의 자산
- 통과 기준: 뒤 단계가 참고할 실물 근거가 문서로 남아 있다. 관행을 문장으로 적을 수 있어야 한다.

### 3. 구성 (내용 기획과 레이아웃 기획을 함께 한다)

구조를 페이지 단위로 내린다. 채움과 품질은 뒤의 게이트가 아니라 이 단계의 기획이 만든다. 게이트에 잡혀서 늘려 채우는 화면은 이미 실패한 화면이다. 페이지마다 네 가지 질문을 차례로 묻고 답을 함께 확정한다.

1. **핵심 메시지 한 줄.** 이 페이지가 증명할 주장이 무엇인가.
2. **레이아웃 패턴.** 이 내용을 전달하려면 택소노미를 어떻게 잡아야 하는가. `layout-taxonomy.md`(원본 분류 체계)에서 패턴 ID 로 지목한다.
3. **인터랙션의 위치.** 그 패턴 안에서 인터랙션이 필요한 곳은 어디인가. 복잡도 사다리의 판정 질문(정지 화면으로 증명되는가, 변화를 보여야 하는가, 체험시켜야 하는가)으로 필요한 곳에만 둔다. 조작이 증명에 기여하지 않으면 정지다.
4. **셀 지도.** 프레임(네 변 여백 80 안쪽)의 어느 구역을 어떤 정보가 채우는가. 어느 셀이 면을 갖고 어느 셀이 배경 위인가. 원고는 이 지도의 분량을 채우도록 쓰게 되므로, 채울 내용이 모자란 지도는 이 단계에서 내용 보강이나 페이지 합치기로 해결한다.

- 산출물: `기획서.md`의 구성 절 (페이지별 핵심 메시지 + 패턴 ID + 인터랙션 위치 판정 + 셀 지도)
- 통과 기준: 페이지 메시지만 이어 읽어도 발표 전체 논리가 통한다 (horizontal logic). 모든 페이지에 패턴 ID 와 셀 지도가 있고, 인터랙션 배치마다 판정 질문의 답이 한 줄 붙어 있다.

### 4. 왕복 루프 (필수, 생략 불가)

구성 결과를 놓고 피드백을 받아 구조 단계로 되돌아간다. 재배치, 삭제, 보강 후 다시 구성한다.

혼자 실행할 때도 왕복은 생략하지 않는다. 자기 구성을 세 각도에서 스스로 친다. **빠진 것**(이 결론을 받으려면 청중이 알아야 하는데 없는 재료), **버릴 것**(역할이 겹치거나 메시지에 기여하지 않는 쪽), **순서**(앞 쪽이 뒤 쪽의 전제를 실제로 깔아 주는가). 친 결과와 고친 내용을 왕복 기록에 남긴다. 사용자 피드백을 받았으면 그것이 우선이고 자가 검토는 그 위에 더한다.

- 산출물: `기획서.md`의 구조 절과 구성 절 개정판, 그리고 왕복 기록 절
- 통과 기준: 최소 1회 왕복이 기록으로 남아 있다. 한 번에 통과한 구조는 검증이 아니라 미검증이다.

### 5. 채널 배분과 인터랙션 명세

3단계에서 정한 인터랙션 위치를 받아 페이지마다 무엇을 말로, 무엇을 인터랙션으로, 무엇을 텍스트로 나눈다. 등급 2~3 페이지는 아래 템플릿으로 인터랙션 명세를 쓴다.

- 산출물: `기획서.md`의 채널 배분 절 (전 페이지 배분표 + 등급 2~3 페이지별 인터랙션 명세)
- 통과 기준: 모든 페이지에 등급과 채널 배분이 있고, 등급 상향마다 "이 메시지가 왜 변화 또는 체험을 요구하는가" 근거 한 줄이 붙어 있다. 한 페이지의 조작 문법이 두 가지를 넘지 않는다.

```markdown
## 페이지 N 인터랙션 명세
- 핵심 메시지: (한 줄)
- 논리 구조와 레이아웃 패턴: (m-* ID, 원본 택소노미 패턴 ID. 변형이면 어느 패턴의 변형인지)
- 조작 문법: (g-* ID 최대 두 가지)
- 등급과 근거: (등급, 이 메시지가 변화 또는 체험을 요구하는 이유 한 줄)
- 상태: (항상 보이는 전역 상태가 무엇이고 어느 칸에 놓이는가)
- 시퀀스: (단계 목록. 클릭당 한 현상 원칙으로 각 단계에서 무엇이 일어나는가. 조작이 셋을 넘으면 단계 점 위치)
- 상세: (요청 시 상세의 조작 문법. 호버 미리보기와 클릭 고정을 짝으로 적는다)
- 해석: (체험 뒤 문장으로 고정하는 takeaway)
- 통제권: (발표자 조작 방법, 키보드 복구 방식, 인터랙션 모드 토글)
- 폴백: (실패 시 보여줄 정적 상태)
- 가드레일: (`interaction-grammar.md`의 항목 가운데 그 페이지에서 특히 걸리는 것. 늘어나는 방향과 칸 예약과 겹침 범위)
```

### 6. 서술 (`outputs` 필수)

**화면에 인쇄될 모든 글자를 이 단계에서 쓴다.** 제목, 부제, 본문, 목록, 카드 안 글자, 조작 안내, 범례, 버튼, 예시 표기까지 전부다. 구현하며 화면을 채우다가 문장을 발명하지 않는다.

**시작하기 전에 `outputs` 의 문서를 연다.** 공용 세 층(`standards/judgment.md`, `standards/korean.md`, `standards/style.md`)에 발표 자료 층(`standards/slides.md`, `standards/genres/presentation.md`)을 더해 적용한다. 내용을 이 스킬로 복사하지 않는다 (bind-not-copy). 그 기준의 골자만 옮기면 이렇다. 슬라이드 제목은 짧은 명사구이고 문장형 제목을 쓰지 않는다. 본문은 한 줄에 한 개념이고 명사로 끊는다. 청중에게 직접 말하는 층과 대본은 경어체다. 세부와 수치는 반드시 원문을 열어 확인한다.

원고는 `templates/manuscript.md` 의 뼈대를 써서 3단계의 셀 지도를 입력으로 받아 셀 단위로 쓴다. 셀 지도가 잡은 분량을 원고가 채워야 하고, 다 쓴 뒤 비는 셀이 나오면 문장을 늘리는 것이 아니라 3단계로 돌아가 지도나 내용을 다시 잡는다. 쪽마다 다음을 각각 적는다. 왼쪽 위 kicker, 제목(강조할 낱말 표시), 부제, 본문(줄바꿈 직접 표시, 액센트 색이 걸리는 구간 표시), 화면 맨 아래 결론 띠, 그 밖 화면에 뜨는 잔 글자.

발표 노트는 별개 산출물이다. 통대본을 쓰지 않고 쪽별 요지와 개념 설명만 적는다.

- 산출물: `슬라이드원고.md`, `발표노트.md`
- 통과 기준: 인쇄 층에 다른 화계가 섞이지 않았다. `standards/banned-words.json` 대조 결과 0건이다. 남은 상투어는 `standards/genres.md` 의 판정표로 판정했고 발표에서 정상인 표현("먼저", "둘째", "흥미로운 점은")을 기계적으로 지우지 않았다. 문장 길이 중앙값이 발표 자료의 실측값 근처에 들고 슬라이드 문장에 쉼표가 없다. 모든 줄에 줄바꿈 위치가 표시돼 있고 7단계의 칸 상한을 넘지 않는다. **시간 적합 게이트**: 노트 기준으로 발표 시간을 추정해 기획에서 확정한 시간과 맞춘다. 초과하면 문장을 압축하는 것이 아니라 구조 단계로 돌아가 재료를 버린다.

### 7. 디자인 시스템 확정 + 목업 선승인 (승인은 사용자)

원고가 먼저 나오고 디자인이 그것을 담는다. 색 토큰, 타이포 스케일, 격자, radius, 모션 규칙, 컴포넌트, 레이아웃 판을 규격으로 짜고 `tokens.css` 로 구현한다. 그 규격으로 대표 페이지 2~3장(정적 1장 + 가장 무거운 인터랙션 1장 포함)을 만들어 사용자 승인을 받는다.

디자인 시스템이 반드시 규정할 것:

- **위계**: 제목과 본문의 크기 차이를 크게 벌린다. 같은 크기가 두 층에 나오면 위계가 무너진다.
- **카드 사용 규칙**: 기본은 배경 위에 내용을 직접 놓는 것이다. 카드는 묶을 이유가 있을 때만 쓰고 페이지 전체를 감싸지 않는다.
- **줄바꿈 상한**: 판마다 그 칸의 글자 수 상한과 권장 줄 수를 정한다. 원고 쓰는 사람이 길이를 알고 쓸 수 있어야 한다.
- **레이아웃 패턴 인스턴스**: 3단계에서 확정한 패턴 ID마다 이 프로젝트의 인스턴스 규격을 쓴다. `layout-taxonomy.md`의 프레임 계약(네 변 여백 80, 12열 격자, 칸 간격 24, 행은 머리·표제·본체 1fr)을 그대로 쓰고, 바꾸면 `tokens.css`가 단일 원본이 되고 무엇을 왜 바꿨는지 남긴다.
- **공통 부품**: `closing-band`·`control-bar`·`well-tray`·`popup-card`·`step-dots`를 판보다 먼저 규격화한다. 판마다 다시 설계하면 같은 부품이 페이지마다 다른 모양이 된다.
- 실물 조사에서 나온 관행을 컴포넌트로 규격화한다.
- **실물 참고**: `ax-education/디자인시스템.md` 와 `ax-education/tokens.css` 가 실전에서 쓴 디자인 시스템이다. 같은 조직 대상 발표면 그대로 인스턴스로 쓰고, 다른 자리면 규격이 무엇을 정하는지의 본보기로만 읽는다. 남의 브랜드 자산을 그 브랜드 발표가 아닌 자리에 쓰지 않는다.

- 산출물: `디자인시스템.md`, `tokens.css`, `deliverables/`의 목업
- 통과 기준: 사용자 선승인. 승인 전에는 대량 제작에 들어가지 않는다 (HUMAN GATE). 쓰이는 판 전부에 인스턴스 규격이 있다.

### 8. 구현

zero-dependency 단일 HTML 로 전체를 제작한다. **`templates/stage.html` 에서 출발한다.** 그 파일은 무대 계약의 구현체다. 배율과 프레임과 키보드 계약과 공용 API(`window.go`, `window.still`, `PAGE` 등록부)가 들어 있고 다섯 해상도에서 검사를 통과한 코드다. 무대 계약을 매번 다시 유도하지 않는다.

뼈대가 주는 것은 거기까지다. **판과 인터랙션은 카탈로그 조립이 아니라 페이지별 명세에서 직접 구현한다.** 뼈대의 예시 쪽을 베껴 화면을 채우지 않는다. 발표자 통제권(키보드 즉시 복구, 인터랙션 모드 토글)과 폴백 정적 상태를 모든 인터랙션에 내장한다.

구현을 마치면 `checklists/build-selfcheck.md` 를 코드와 대조해 전 항목을 통과시킨다. 이것은 관찰을 대신하지 못하고 관찰 앞에서 걸러 내는 그물이다.

`interaction-grammar.md`의 안정성 가드레일 아홉 가지(`reserved-slot`·`growth-direction`·`no-position-shift`·`keyboard-recovery`·`hover-response`·`static-fallback`·`stage-reset`·`stacking-discipline`·`self-contained`)를 전 페이지에 적용한다. 선택 항목이 아니라 기본이다. `overflow-containment` 와 `intrinsic-sizing` 과 `cls-prevention` 은 원본 택소노미가 갖고 함께 적용한다.

- 산출물: `deliverables/`의 발표 HTML (파일명은 발표 제목)
- 통과 기준: file://로 열리고, 처음부터 끝까지 키보드만으로 진행되며, 모든 인터랙션이 명세의 상태, 시퀀스, 상세, 해석, 폴백을 실제로 구현했다. 웹폰트와 이미지가 인라인돼 있어 HTML 파일 하나만 옮겨도 같은 화면이 나온다. 자가 점검표 실패 0건이다.

### 9. 검증

검증은 세 갈래이고 셋을 다 통과해야 완료다.

**글자 검수.** `outputs` 의 `guides/review.md` 절차로 검수한다. 그 문서가 정한 대로 내용 검수와 표현 검수를 따로 진행한다. 새 대화에 넘길 수 없으면 직접 하되 두 검수를 한 번에 섞지 않고, 검수자 자리에서는 고치지 않고 위치와 이유만 먼저 적는다.

**화면 검증.** 브라우저를 띄울 수 있으면 해상도 매트릭스 게이트(`tools/fluid-gate.mjs`)를 돌린다. 덱 경로를 인자로 준다 (`node tools/fluid-gate.mjs <덱 경로>`, playwright 필요). 다섯 해상도(4:3, 16:10, 16:9, 21:9, 21:9 고해상도)에서 스테이지 폭 계산, 렌더 배율, 여백 80 유지, 정지 상태와 End 완료 상태의 네 변 넘침, 호버 전수 반응을 검사한다. 덱은 게이트가 전제하는 공용 API(`.slide` 쪽 목록, `window.go` 0 기반, `window.still`, End 완료 상태)를 갖춰야 한다. `templates/stage.html` 로 시작했으면 이미 갖춰져 있다.

**조작 관찰.** 등급 2~3 인터랙션은 실제 조작을 관찰해야 완료다. 브라우저를 띄울 수 없는 자리에서는 관찰하는 눈이 사용자다. `checklists/observation-request.md` 로 쪽마다 무엇을 눌러 무엇이 보여야 하는지 적어 사용자에게 넘기고, 어긋난 항목을 고친 뒤 다시 확인받는다. 게이트를 못 돌렸다는 사실도 함께 밝힌다. 게이트를 돌리지 못한 것과 통과한 것을 같은 말로 적지 않는다.

마지막으로 상용 레퍼런스와 같은 배율로 나란히 놓아 "옆에 놓아도 어색하지 않은가" 로 판정한다.

- 산출물: 검수 기록, 게이트 결과 또는 관찰 답변, 페이지별 판 지도
- 통과 기준: 글자 검수 통과. 게이트 실패 0건 또는 관찰 항목 전건 확인. 등급 2~3 전 인터랙션의 조작 관찰 증거가 있다. 페이지마다 어떤 판을 왜 골랐는지 한 줄씩 적힌 지도가 있고, 그 지도와 실제 구현이 어긋나지 않는다.

### 10. 회고·승격 (발표 후 선택)

실전에서 만든 인터랙션 구현체 중 재사용 가치가 있는 것을 검토해 승격 후보로 표시한다. 인터랙션 카탈로그는 이 실전 승격으로만 자란다.

- 산출물: `기획서.md`의 회고 절 (승격 후보 표시와 해당 구현체·명세의 위치 기록)
- 통과 기준: 사용자가 승격 여부를 결정할 수 있는 형태의 후보 목록이다.

## 기술 기본값

- 무대 계약의 구현체는 `templates/stage.html` 이다. 아래 값들은 그 파일에 이미 들어 있다.
- **마진 보존 가변 폭 스테이지**의 **zero-dependency 단일 HTML**. 빌드 없이 file://로 열리는 자기완결 파일이다. 배율은 min(vw/1920, vh/1080) 이고 설계 폭은 1920~2560 가변, 설계 높이는 1080 고정이다. 화면비가 맞지 않으면 레터박스를 남기고 레터박스 색은 그 쪽의 캔버스 색이다. 네 변 여백은 80 이고 그 안쪽은 `layout-taxonomy.md` 의 프레임 계약과 벤토 채움을 따른다.
- 폰트는 Pretendard, Noto Sans KR 순으로 선언하고 `word-break: keep-all`을 기본 적용한다. 폰트 로드가 실패해도 시스템 산세리프로 자연스럽게 내려가는 스택으로 쓴다.
- **웹폰트와 이미지는 base64로 인라인한다.** CDN이나 형제 폴더를 참조하면 발표장 네트워크가 없거나 파일 하나만 옮겼을 때 서체가 폴백으로 내려가고, 서체가 바뀌면 원고의 줄 나눔 규격이 전부 무효가 된다. 한글 가변 폰트는 실제 쓰는 글자로 서브셋해 넣고(`pyftsubset --flavor=woff2`로 축 범위를 유지한다) 서브셋 스크립트를 `qa/`에 남긴다.
- 인터랙션은 **same-document 컴포넌트**로 구현한다. iframe은 키보드 포커스를 삼키므로 금지다.
- 모든 인터랙션 조작 후 **키보드 내비게이션이 즉시 복구**된다. 인터랙션 모드의 진입과 이탈은 명시 토글로 한다.
- 차트는 인라인 SVG를 우선하고, 단계 공개와 상태 전환은 CSS 클래스 토글과 상태 머신 JS로 구현한다. 무거운 라이브 데모가 필요할 때만 레포형 산출물로 승급을 검토하며, 이는 사용자 확인 사항이다.
- 산출물은 언제나 HTML이다. 이 스킬에 PDF 내보내기는 없다.

## 도구 메모

전부 기존 자료를 **읽기** 위한 것이다.

- 지난 발표 자료가 PDF로만 남아 있으면 이미지로 변환해 눈으로 본다. poppler가 없으면 PyMuPDF(`import fitz`)를 쓴다.
- PPTX는 zip이다. `ppt/slides/slide*.xml`에서 `typeface=`와 `srgbClr val=`을 세면 그 템플릿이 실제로 쓴 서체와 팔레트가 나온다.
- 기업 전용 서체는 TTF가 크다. `fonttools`로 woff2 변환하면 영문 전용은 60KB 안팎이 되어 파일에 실을 수 있고, 국문은 쓰는 글자만 서브셋해야 한다.
- 렌더 검증은 브라우저 자동화로 한다. 로컬 파일을 `file://` 로 열고, 조작은 `window.go` 와 `PAGE[n].step()` 을 직접 호출해 단계별 상태를 만든 뒤 스크린샷으로 확인한다. 자동화를 쓸 수 없으면 9단계의 조작 관찰로 대신한다.
