---
name: human-presentation-interaction
description: (마당) 발표의 목적과 메시지를 먼저 잡고, 청중이 직접 보고 조작하며 이해하게 되는 발표 자료를 기획부터 구현과 검증까지 만들어 줍니다. "인터랙티브 발표 만들자", "체험형 프레젠테이션", "조작하는 슬라이드", "발표에 인터랙션 넣자" 같은 요청에 사용합니다.
---

# human-presentation-interaction (인터랙티브 프레젠테이션 제작)

개념을 도형 몇 개로 그려 넣는 대신, 그 개념에 어떤 인터랙션이 필요한지 먼저 판정하고 실제 웹에서 구동되는 구현체 수준까지 만들어내는 스킬이다. 품질 기준의 실물 표본은 Claude Code 공식 문서의 "Explore the context window" 위젯이며, 그 해부와 지향점은 `research/north-star.md`에 있다. 발표 철학의 실측 근거는 `research/daehyeon-presentation-philosophy.md`, 인터랙션 함정과 방법론 근거는 `research/presentation-landscape.md`다. 세 문서는 필요할 때 Read하는 참조 소스이고, 이 스킬 본문이 실행 절차의 SSOT다. 트리거 어휘: "인터랙티브 발표", "체험형 프레젠테이션", "인터랙티브 슬라이드", "발표에 인터랙션 넣자".

레이아웃의 SSOT는 `layout-taxonomy.md`다. 조작 문법과 메시지의 논리 구조를 교차해 판 16종을 정의하고, 판마다 1920x1080 절대 좌표 규격과 조합 규칙과 복잡도 등급 대응을 갖는다. 안정성 가드레일 여덟 가지도 그 문서가 갖는다. 3단계 구성과 5단계 채널 배분과 7단계 디자인 시스템과 8단계 구현이 그 문서의 패턴 ID로 레이아웃을 지목한다. 전역 `~/.claude/skills/layout-taxonomy/taxonomy.md`는 스크롤 웹 UX용이라 그대로 끌어오지 않는다.

## 핵심 원칙 (계약)

1. **인터랙션이 곧 논증이다.** 페이지의 핵심 메시지는 텍스트로 주장되기 전에 조작과 변화로 체험된다. 메시지 전달에 기여하지 않는 장식용 인터랙션은 넣지 않고, 체험 뒤에는 해석 문장(takeaway)으로 못박는다.
2. **콘텐츠 설계가 인터랙션 설계다.** 어떤 단계로 끊고 어떤 항목에 어떤 설명을 붙일지는 전부 내용 결정이다. 인터랙션 뼈대를 먼저 만들고 내용을 붓지 않는다. 내용의 논리 구조가 인터랙션 구조를 결정한다.
3. **복잡도는 메시지가 요구할 때만 올린다.** 페이지마다 복잡도 사다리의 판정 질문을 통과한 등급만 쓴다. 등급 상향에는 근거 한 줄이 붙는다.
4. **페이지당 핵심 메시지 하나.** 메시지 한 줄을 먼저 쓰고 레이아웃은 그 메시지의 논리 구조를 시각화하는 수단으로 고른다. 메시지 없는 페이지는 만들지 않는다.
5. **화면에 인쇄되는 글자는 쓰는 담당이 쓴다.** 구현자는 원고의 글자를 화면에 앉히기만 하고 새 이름이나 새 문장을 발명하지 않는다. 원고가 먼저 나오고 구현이 뒤따른다. 이 순서를 어기면 화면에서 문장이 발명되고 전면 재작업이 된다.
6. **층을 가른다.** 화면에 인쇄되는 글자와 기획·노트의 메모 층은 다른 문서이고 화계도 다르다. 인쇄 층의 화계는 장르가 아니라 **청중과의 관계**가 정한다. 동료 대상 세미나면 개조식이 맞고, 외부 청중에게 직접 말하는 자리면 경어체다. 청중을 확인하기 전에 화계를 고정하지 않는다.
7. **줄바꿈은 쓰는 사람이 정한다.** 화면 폭에 맡기지 않는다. 원고의 줄 나눔이 화면의 줄 나눔이고 한 줄이 한 호흡이다. 넘치면 줄이는 것이 아니라 들어갈 길이를 알고 쓴다.
8. **근거 없는 주장 금지.** 수치, 인용, 사실 주장은 수집 시점부터 출처를 함께 붙인다. 출처 없는 재료는 [미확정] 표시로만 남고 슬라이드에 오르지 못한다. 시연과 실습의 소재는 가상으로 만들고 화면에 예시임을 표기한다.
9. **발표자 통제권과 폴백.** 모든 인터랙션은 무대에서 한 손으로 조작 가능하고(클릭 타깃 크게), 조작 후 키보드 내비게이션이 즉시 복구되며, 실패 시 보여줄 정적 폴백 상태를 갖는다. 인터랙션이 발표를 끌고 가지 않는다.
10. **완료 판정은 렌더 관찰과 조작 관찰이다.** file://로 실제 구동한 스크린샷과 조작 확인만 완료 증거다. 코드 검토나 정적 검사만으로 완료를 선언하지 않는다.

## 인터랙션 복잡도 사다리 (판정 기준표)

페이지마다 "이 페이지의 핵심 메시지는 정지 화면으로 증명되는가, 변화를 보여야 하는가, 체험시켜야 하는가"를 묻고 등급을 정한다. 근거는 `research/north-star.md`다.

| 등급 | 형태 | 언제 |
|---|---|---|
| 0 정적 | 잘 설계된 다이어그램, 표 | 구조가 정지 상태로 다 보일 때 |
| 1 공개 | 호버 공개, 클릭 팝업, 토글 | 상세를 숨겨 인지 부하를 줄일 때 (detail-on-demand) |
| 2 단계 | fragment 진행, 상태 전환, 비교 슬라이더 | 변화, 대비, 과정이 메시지일 때 |
| 3 시뮬레이션 | 재생 가능한 모델 + 탐색 + 인스펙터 | 시스템의 동작 자체가 메시지일 때 |

## 앵커링 경계

- `design-decks`와 `labsem-ppt`의 절차, 미감, 템플릿을 기본값으로 가져오지 않는다. 정적 슬라이드 생성기의 관행을 끌어오면 지향점이 하향 앵커링된다. 검증된 개별 역학(1920x1080 고정 스테이지 스케일링 CSS, file:// 렌더 검증 규율 등)만 의식적으로 골라 쓰고, 절차와 스타일은 이 스킬이 스스로 정의한다.
- 인터랙션 asset 카탈로그를 선제 제작하지 않는다. 카탈로그는 실전에서 만든 구현체의 회고와 승격으로만 자란다.

## 산출물 배치

발표 프로젝트 폴더는 파이프라인 단계가 아니라 사용자의 사용 흐름을 기준으로 나눈다. 사고와 결정은 `기획서.md` 한 문서에 절로 쌓이고, 폴더는 재료와 결과물과 증거의 최소 구분만 둔다. 각 항목은 "사용자가 왜 여길 여는가"로 설명된다.

```text
기획서.md        발표의 사고와 결정 전부 (기획, 구조, 구성, 왕복 기록, 채널 배분). 왜 이렇게 만들었는지 보러 연다
슬라이드원고.md   화면에 인쇄될 모든 글자. 화면에 뭐라고 쓰였는지 보러 연다
발표노트.md       쪽별 요지와 개념 설명. 발표 준비하러 연다
디자인.md         사용자가 준 시각 기준. 무엇을 지켜야 하는지 보러 연다
디자인시스템.md    구현 규격 (색·타이포·격자·컴포넌트·레이아웃 판)
tokens.css        값의 단일 원본. 각 파일이 이 내용을 복사해 쓴다
인터랙션.md       조작이 어떻게 동작하는지 보러 연다
reference/       원천·청중·브랜드·기존 자료 조사. 근거를 확인하러 연다
assets/          로고, 웹폰트, 배경 이미지
deliverables/    선승인 목업과 최종 발표 HTML. 발표를 띄우러 연다
qa/              렌더 캡처와 조작 관찰 증거. 완료 판정 근거를 보러 연다
```

발표가 길어 파일을 나누면 `deliverables/`에 단위별로 하나씩 둔다. 각 파일은 독립 실행된다.

각 단계는 이 배치 안에 자기 산출물을 남기고 통과 기준을 만족한 뒤에 다음 단계로 간다.

## 절차

담당이 레서(leader-writing)나 나비(leader-design)인 단계는 해당 leader를 Agent로 디스패치해 수행한다. 나머지는 이 스킬을 실행하는 메인 루프가 직접 한다.

### 1. 기획 (담당: 메인 루프)

발표의 목적, 청중, 전체 핵심 메시지, 발표 상황(시간, 말로 커버할 범위)을 사용자 인터뷰로 확정한다. 결론 한 줄과 청중 체험 동선(이 발표를 들은 사람이 무엇을 가지고 나가는가)까지 잡는다.

- 산출물: `기획서.md`의 기획 절
- 통과 기준: 결론 한 줄, 발표 시간, 청중 체험 동선이 문서에 확정돼 있고 사용자가 확인했다.

### 2. 구조 (담당: 메인 루프)

목차 그 이상의 내용 구조를 두 국면으로 짠다.

- **발산**: 메시지를 전달하기 위해 담아야 할 내용 후보를 브레인스토밍과 자료 수집으로 포화시킨다. 새 것이 안 나올 때까지 파고, 닫기 전에 "빠진 것이 없는가"를 되묻는다. 수치, 인용, 사실 주장은 출처를 함께 수집한다.
- **배치**: 수집된 재료를 메시지 전달의 논리로 배열한다. 각 재료의 역할(훅, 정의, 대비, 근거, 체험, 성찰)을 정하고 무엇을 버릴지 결정한다. 구조를 먼저 고정하고 표면은 나중에 간다.

- 산출물: `기획서.md`의 구조 절 (재료 목록과 역할 배치). 수집한 원자료와 출처는 `reference/`에 둔다.
- 통과 기준: 남은 재료 전부에 역할이 있고, 수치와 인용에 출처가 붙어 있거나 [미확정]으로 표시돼 있다. 근거 없는 재료는 이 단계를 넘어가지 못한다.

### 2.5 실물 조사 (담당: 메인 루프. 생략하면 뒤가 전부 반려된다)

규칙 문서만 읽고 시작하지 않는다. 실물을 눈으로 본다.

- **사용자의 지난 발표 자료를 렌더 이미지로 직접 본다.** 여러 편을 보고 구조, 강조 방식, 줄바꿈, 위계의 관행을 뽑아 `reference/`에 남긴다. HTML로 남아 있으면 렌더 캡처를, PDF로만 남아 있으면 이미지로 변환해서 본다.
- **발표가 특정 조직을 향하면 그 조직의 브랜드 자산과 실물 화면도 본다.** 공식 템플릿 파일이 있으면 열어서 실제 쓰인 서체와 색을 세어 확인한다. 추정하지 않는다.
- 로고와 서체 같은 브랜드 자산은 사용자에게 받는다. 서드파티 브랜드 자산을 직접 그리지 않는다.

- 산출물: `reference/`의 조사 문서, `assets/`의 자산
- 통과 기준: 뒤 단계가 참고할 실물 근거가 문서로 남아 있다. 관행을 문장으로 적을 수 있어야 한다.

### 3. 구성 (담당: 메인 루프)

구조를 페이지 단위로 내린다. 페이지마다 핵심 메시지 한 줄을 먼저 쓰고, 그 메시지의 논리 구조를 레이아웃의 뼈대로 삼는다.

논리 구조는 `layout-taxonomy.md`의 아홉 가지(`m-list`·`m-order`·`m-contrast`·`m-process`·`m-accumulate`·`m-decompose`·`m-relation`·`m-distribution`·`m-declare`)에서 고른다. 이 단계에서는 논리 구조 ID까지만 확정하고 판 ID는 5단계에서 등급과 함께 정한다. 등급을 모르는 상태로 판을 먼저 고르면 메시지가 아니라 화면이 등급을 정하게 된다.

- 산출물: `기획서.md`의 구성 절 (페이지별 핵심 메시지 목록 + 논리 구조 ID)
- 통과 기준: 페이지 메시지만 이어 읽어도 발표 전체 논리가 통한다 (horizontal logic 게이트). 모든 페이지에 논리 구조 ID가 붙어 있다.

### 4. 왕복 루프 (필수, 생략 불가. 담당: 메인 루프 진행, 피드백은 사용자와 레서·나비 시점 검토)

구성 결과를 놓고 피드백을 받아 구조 단계로 되돌아간다. 재배치, 삭제, 보강 후 다시 구성한다. 피드백 주체는 사용자 그리고/또는 비판 관점의 검토(레서와 나비 시점 포함)다.

- 산출물: `기획서.md`의 구조 절과 구성 절 개정판, 그리고 왕복 기록 절
- 통과 기준: 최소 1회 왕복이 기록으로 남아 있다. 한 번에 통과한 구조는 검증이 아니라 미검증이다.

### 5. 채널 배분 (담당: 메인 루프)

페이지마다 무엇을 말로, 무엇을 인터랙션으로, 무엇을 텍스트로 나눈다. 인터랙션이 필요한 페이지는 복잡도 사다리로 등급을 판정하고, 등급 2~3은 아래 템플릿으로 인터랙션 명세를 쓴다.

등급을 정한 뒤 `layout-taxonomy.md`의 판 고르는 절차를 밟아 판 ID를 확정한다. 논리 구조와 등급으로 교차표에서 후보를 뽑고, Best For와 Avoid For로 거르고, 앞뒤 페이지의 판과 조합 규칙을 대조한다. 후보가 남지 않으면 가장 가까운 판의 변형으로 만들고 어느 판의 변형인지 명세에 적는다.

- 산출물: `기획서.md`의 채널 배분 절 (전 페이지 배분표 + 등급 2~3 페이지별 인터랙션 명세)
- 통과 기준: 모든 페이지에 등급과 채널 배분과 판 ID가 있고, 등급 상향마다 "이 메시지가 왜 변화 또는 체험을 요구하는가" 근거 한 줄이 붙어 있다. 한 페이지의 조작 문법이 두 가지를 넘지 않는다.

```markdown
## 페이지 N 인터랙션 명세
- 핵심 메시지: (한 줄)
- 논리 구조와 판: (m-* ID, 판 ID. 변형이면 어느 판의 변형인지)
- 조작 문법: (g-* ID 최대 두 가지)
- 등급과 근거: (등급, 이 메시지가 변화 또는 체험을 요구하는 이유 한 줄)
- 상태: (항상 보이는 전역 상태가 무엇이고 어느 칸에 놓이는가)
- 시퀀스: (단계 목록. 클릭당 한 현상 원칙으로 각 단계에서 무엇이 일어나는가. 조작이 셋을 넘으면 단계 점 위치)
- 상세: (요청 시 상세의 조작 문법. 호버 미리보기와 클릭 고정을 짝으로 적는다)
- 해석: (체험 뒤 문장으로 고정하는 takeaway)
- 통제권: (발표자 조작 방법, 키보드 복구 방식, 인터랙션 모드 토글)
- 폴백: (실패 시 보여줄 정적 상태)
- 가드레일: (그 페이지에서 특히 걸리는 항목. 늘어나는 방향과 칸 예약과 겹침 범위)
```

### 6. 서술 (담당: 레서, leader-writing)

**화면에 인쇄될 모든 글자를 레서가 쓴다.** 제목, 부제, 본문, 목록, 카드 안 글자, 조작 안내, 범례, 버튼, 예시 표기까지 전부다. 구현자가 화면을 채우며 문장을 발명하지 않는다. 문체 기준은 `~/projects/daehyeon-voice/packs/ppt-narration.md`를 이 단계에서 Read해 적용하고 내용을 이 스킬로 복사하지 않는다 (bind-not-copy). 금지 표현 목록의 SSOT는 `~/projects/daehyeon-voice/fingerprint/ppt-narration.json`이다.

원고는 쪽마다 다음을 각각 적는다. 왼쪽 위 kicker, 제목(강조할 낱말 표시), 부제, 본문(줄바꿈 직접 표시, 액센트 색이 걸리는 구간 표시), 화면 맨 아래 결론 띠, 그 밖 화면에 뜨는 잔 글자.

발표 노트는 별개 산출물이다. 통대본을 쓰지 않고 쪽별 요지와 개념 설명만 적는다.

- 산출물: `슬라이드원고.md`, `발표노트.md`
- 통과 기준: 인쇄 층에 다른 화계가 섞이지 않았고 금지 표현이 0건이다. **시간 적합 게이트**: 노트 기준으로 발표 시간을 추정해 기획에서 확정한 시간과 맞춘다. 초과하면 문장을 압축하는 것이 아니라 구조 단계로 돌아가 재료를 버린다.

### 7. 디자인 시스템 확정 + 목업 선승인 (담당: 나비 leader-design + 메인 루프, 승인은 사용자)

원고가 먼저 나오고 디자인이 그것을 담는다. 나비가 색 토큰, 타이포 스케일, 격자, radius와 그림자, 모션 규칙, 컴포넌트, 레이아웃 판을 규격으로 짜고 `tokens.css`로 구현한다. 메인 루프가 그 규격으로 대표 페이지 2~3장(정적 1장 + 가장 무거운 인터랙션 1장 포함)을 만들어 사용자 승인을 받는다.

디자인 시스템이 반드시 규정할 것:

- **위계**: 제목과 본문의 크기 차이를 크게 벌린다. 같은 크기가 두 층에 나오면 위계가 무너진다.
- **카드 사용 규칙**: 기본은 배경 위에 내용을 직접 놓는 것이다. 카드는 묶을 이유가 있을 때만 쓰고 페이지 전체를 감싸지 않는다.
- **줄바꿈 상한**: 판마다 그 칸의 글자 수 상한과 권장 줄 수를 정한다. 원고 쓰는 사람이 길이를 알고 쓸 수 있어야 한다.
- **레이아웃 판**: 5단계에서 확정한 판 ID마다 이 프로젝트의 인스턴스 규격을 쓴다. `layout-taxonomy.md`의 기준 좌표와 열 격자를 그대로 쓰거나, 바꾸면 `tokens.css`가 단일 원본이 되고 무엇을 왜 바꿨는지 남긴다.
- **공통 부품**: `closing-band`·`control-bar`·`well-tray`·`popup-card`·`step-dots`를 판보다 먼저 규격화한다. 판마다 다시 설계하면 같은 부품이 페이지마다 다른 모양이 된다.
- 실물 조사에서 나온 관행을 컴포넌트로 규격화한다.

- 산출물: `디자인시스템.md`, `tokens.css`, `deliverables/`의 목업
- 통과 기준: 사용자 선승인. 승인 전에는 대량 제작에 들어가지 않는다 (HUMAN GATE). 쓰이는 판 전부에 인스턴스 규격이 있다.

### 8. 구현 (담당: 메인 루프)

zero-dependency 단일 HTML(1920x1080 고정 스테이지)로 전체를 제작한다. 인터랙션은 카탈로그 조립이 아니라 페이지별 명세에서 직접 구현하고, 발표자 통제권(키보드 즉시 복구, 인터랙션 모드 토글)과 폴백 정적 상태를 모든 인터랙션에 내장한다.

`layout-taxonomy.md`의 안정성 가드레일 여덟 가지(`overflow-containment`·`reserved-slot`·`growth-direction`·`no-position-shift`·`keyboard-recovery`·`static-fallback`·`stage-reset`·`self-contained`)를 전 페이지에 적용한다. 선택 항목이 아니라 기본이다.

- 산출물: `deliverables/`의 발표 HTML (파일명은 발표 제목)
- 통과 기준: file://로 열리고, 처음부터 끝까지 키보드만으로 진행되며, 모든 인터랙션이 명세의 상태, 시퀀스, 상세, 해석, 폴백을 실제로 구현했다. 웹폰트와 이미지가 인라인돼 있어 HTML 파일 하나만 옮겨도 같은 화면이 나온다.

### 9. 검증 (담당: 나비 leader-design + 메인 루프)

나비의 비주얼 QA(렌더 스크린샷)와 slop 검증(텍스트는 slop-check, 디자인은 anti-slop 기준)을 통과한다. 등급 2~3 인터랙션은 실제 조작 관찰까지 확인한다. 상용 레퍼런스와 같은 배율로 나란히 놓아 "옆에 놓아도 어색하지 않은가"로 판정한다.

- 산출물: `qa/` 렌더 캡처와 조작 관찰 증거, 판정 기록, 페이지별 판 지도
- 통과 기준: 나란히 비교 판정 통과 + 등급 2~3 전 인터랙션의 조작 관찰 증거가 남아 있다. 페이지마다 어떤 판을 왜 골랐는지 한 줄씩 적힌 지도가 있고, 그 지도와 실제 구현이 어긋나지 않는다.

### 10. 회고·승격 (발표 후 선택. 담당: 메인 루프)

실전에서 만든 인터랙션 구현체 중 재사용 가치가 있는 것을 검토해 승격 후보로 표시한다. 인터랙션 카탈로그는 이 실전 승격으로만 자란다.

- 산출물: `기획서.md`의 회고 절 (승격 후보 표시와 해당 구현체·명세의 위치 기록)
- 통과 기준: 사용자가 승격 여부를 결정할 수 있는 형태의 후보 목록이다.

## 기술 기본값

- **마진 보존 가변 폭 스테이지**의 **zero-dependency 단일 HTML**. 빌드 없이 file://로 열리는 자기완결 파일이다. 세로는 1080 기준 배율로 뷰포트 높이에 맞추고, 가로는 네 변 여백 80 을 고정한 채 무대 폭이 뷰포트를 채운다(폭 하한 1920, 상한 2560). 16:9 한 사이즈에 레터박스로 맞추지 않고 넓은 화면에서는 인터랙션이 넓어진 폭을 쓴다. 좌표 규격은 `layout-taxonomy.md` 의 가변 폭 규칙을 따른다.
- 폰트는 Pretendard, Noto Sans KR 순으로 선언하고 `word-break: keep-all`을 기본 적용한다. 폰트 로드가 실패해도 시스템 산세리프로 자연스럽게 내려가는 스택으로 쓴다.
- **웹폰트와 이미지는 base64로 인라인한다.** CDN이나 형제 폴더를 참조하면 발표장 네트워크가 없거나 파일 하나만 옮겼을 때 서체가 폴백으로 내려가고, 서체가 바뀌면 원고의 줄 나눔 규격이 전부 무효가 된다. 한글 가변 폰트는 실제 쓰는 글자로 서브셋해 넣고(`pyftsubset --flavor=woff2`로 축 범위를 유지한다) 서브셋 스크립트를 `qa/`에 남긴다.
- 인터랙션은 **same-document 컴포넌트**로 구현한다. iframe은 키보드 포커스를 삼키므로 금지다.
- 모든 인터랙션 조작 후 **키보드 내비게이션이 즉시 복구**된다. 인터랙션 모드의 진입과 이탈은 명시 토글로 한다.
- 차트는 인라인 SVG를 우선하고, 단계 공개와 상태 전환은 CSS 클래스 토글과 상태 머신 JS로 구현한다. 무거운 라이브 데모가 필요할 때만 레포형 산출물로 승급을 검토하며, 이는 사용자 확인 사항이다.
- 산출물은 언제나 HTML이다. 이 스킬에 PDF 내보내기는 없다.

## 도구 메모

전부 기존 자료를 **읽기** 위한 것이다.

- 지난 발표 자료가 PDF로만 남아 있으면 이미지로 변환해 눈으로 본다. poppler가 없으면 PyMuPDF(`import fitz`)를 쓴다.
- PPTX는 zip이다. `ppt/slides/slide*.xml`에서 `typeface=`와 `srgbClr val=`을 세면 그 템플릿이 실제로 쓴 서체와 팔레트가 나온다.
- 기업 전용 서체는 TTF가 크다. `fonttools`로 woff2 변환하면 영문 전용은 60KB 안팎이 되어 파일에 실을 수 있고, 국문은 쓰는 글자만 서브셋해야 한다.
- 렌더 검증은 gstack `browse`로 한다. 로컬 파일은 `goto file://...`로 열고, 조작은 `js`로 함수를 직접 호출해 단계별 상태를 만든 뒤 스크린샷으로 확인한다.
- 리더 에이전트가 모델 사용 한도에 걸리면 `model: opus`로 재디스패치한다.
